# TESTING — the media engine

The governing verification document for the Rust engine. When a change touches
a row's surface, run that row and say so when claiming "verified" — a row that
was not run is not a row that passed.

The managed half has its own companion at [`../TESTING.md`](../TESTING.md),
covering what lives above the ABI: the panel, the session cap, shared playback
and the prefabs, several of which need two clients or a populated scene and so
have no automated row anywhere. Between them they cover the package; neither is
complete on its own.

Rows are grouped by the part of the pipeline they exercise, so a change picks
its rows by where it landed. Anything whose surface exists but has never been
exercised sits under [Not yet run](#not-yet-run) instead of among the rows that
have, so reading the matrix does not imply coverage that is not there.

## Gate 0 — local CI (every commit)

```
.\tools\ci.ps1        # Windows (ci.sh on Linux/WSL)
```

fmt, clippy `-D warnings` (including `undocumented_unsafe_blocks`), the
full test suite, cargo-deny (licence + advisories), cargo-vet, and the
ffprobe conformance gate over `fixtures/` (skipped loudly if ffprobe is
not on PATH). Everything below assumes this is green.

**Toolchain baseline: 1.98.0**, pinned by `rust-toolchain.toml` along with
`clippy`, `rustfmt` and the `aarch64-linux-android` target, so a fresh
checkout provisions the same set and the Android lane is not skipped for
want of a target. Because the gate denies warnings, an unpinned channel
would let each new clippy lint fail the build on Rust's release schedule
rather than ours, and green would stop meaning the same thing on two
machines. Bumping is a deliberate commit: raise the channel, run the gate,
fix what the new lints surface, and update this baseline in the same change.

## Diagnostics surfaces — what to read, and what good looks like

Three surfaces, deliberately not overlapping. Grading a run means reading
numbers off them, not watching it.

| Surface | Written by | Sampled | Covers |
| --- | --- | --- | --- |
| **Engine capture** | the engine, own thread | 100 ms | Everything inside the pipeline: stage counters, bank depth and lag, decode, release, events. Column contract pinned by spec §12.4. Turned on per session: descriptor `diag_csv`, managed `diagnosticsCsvPath`, `bm-probe play --csv`. |
| **Frame capture** | `BasisMediaPlayerDiagnostics`, main thread | one row per Unity frame | The other side of the boundary, which the engine cannot see: render cadence, how long each frame was held, audio pull rate, what the per-speaker outputs consumed and how loud it was, the device's DSP chain. Sandboxed to `persistentDataPath`. |
| **Debug window** | editor, live | 250 ms | The same numbers while it is happening, walked in pipeline order, for when the question is "which stage stopped". `Basis > Debug > Media Player`. |

The two captures share a time base, so they are read side by side: the engine
capture says what the pipeline did, the frame capture says what the viewer got.

### Frame capture column contract

Positional. Additions go on the end, so a reader keyed on position stays valid.
This line must match `BasisMediaPlayerDiagnostics.Header()` exactly — if it does
not, one of the two is wrong and the grading below is measuring the wrong column:

```
unity_time,frame,frame_dt_ms,state,position_us,duration_us,banked_ms,decoded,decoded_delta,presented,presented_delta,frames_held,video_w,video_h,has_texture,audio_pulled,audio_pulled_delta,stream_rate,stream_channels,dsp_rate,dsp_buffer,dsp_buffers,listener_paused,sync_ppm,subtitle_track,caption_len,out_bound,out_playing,out_consumed,out_consumed_delta,out_peak,out_rms,out_latency_us
```

### What each metric means, and its healthy band

| Column | Means | Healthy |
| --- | --- | --- |
| `state` | `BmState`: 0 idle, 1 opening, 2 buffering, 3 playing, 4 paused, 5 ended, 6 error | reaches 3 and stays; 6 never appears; a VOD run ends at 5 |
| `position_us` | the session clock | advances at 1 s per wall second (±2%, the slew cap). **Never goes backwards outside a seek** — a backwards step is a clock snap, and snaps are the thing pacing bugs show up as |
| `banked_ms` | how much is banked ahead | 0 while buffering; at or above the configured depth once playing (Auto lanes settle wherever the estimator lands). Sagging towards 0 during playback is a starving source |
| `decoded_delta` | frames decoded this Unity frame | over a window, sums to the source frame rate |
| `presented_delta` | frames presented this Unity frame | over a window, matches `decoded_delta`. Persistently below it means frames are being dropped after decode |
| `frames_held` | Unity frames the last presented frame stayed on screen (0 on frames where nothing new was presented) | the display-to-content ratio, and *the same value every time*: 24 fps on a 72 Hz headset is 3, 3, 3. The share of holds at the ideal value is the judder metric — **≥99% on a steady lane** was the bar the frame-pacing work was held to. Isolated 2s and 4s are single-vsync jitter; clusters are a regression |
| `frame_dt_ms` | Unity's own frame time | the display interval (13.9 ms at 72 Hz, 16.7 at 60). If this is unstable the hold histogram means nothing — the renderer is the problem, not the player |
| `audio_pulled_delta` | source frames consumed this Unity frame | over a window, **`audio_pulled` must advance at `stream_rate`, whatever `dsp_rate` is** — the pull resamples. A pull rate stuck at some ratio of the stream rate drags the master clock and everything else follows |
| `stream_rate` / `stream_channels` | what the engine announced | non-zero once audio is announced; channels matching the source |
| `dsp_rate`, `dsp_buffer`, `dsp_buffers` | the device's audio chain | context for the above, not a pass/fail. Worth recording because devices pick their own (a Quest picks 24 kHz on its own) |
| `sync_ppm` | the sync ladder's wanted offset from 1x | 0 unless chasing a shared position; non-zero for long stretches means the correction is not converging |
| `has_texture` | the output texture exists | 1 shortly after the first frame; dropping back to 0 mid-playback is a lost texture |
| `listener_paused` | `AudioListener.pause` | 0. If it is 1, audio metrics are meaningless and the clock will have fallen back to wall |
| `subtitle_track`, `caption_len` | selected sidecar track (-1 = in-band), current caption length | context for caption work |
| `out_bound` | AudioSources the audio sink bound at its last build | the number of outputs the rig wires — 1 for a stereo downmix, 8 for the surround arrangement. **0 with audio announced means nothing was listening**: the ring was drained and thrown away, which every other band still reads as healthy |
| `out_playing` | any bound output is playing | 1 once the format is known and the outputs start. 0 while playing is the outputs having stopped under a session that has not |
| `out_consumed_delta` | output frames the primary output mixed this Unity frame | over a window, `out_consumed` advances at **`dsp_rate`**, not `stream_rate` — the taps render into DSP blocks, so this is the device's clock. Below it means blocks were missed, which is audible as break-up |
| `out_peak`, `out_rms` | level of the primary output's last mixed block | non-zero on anything that is not silence. A run where every block is 0 played nothing, however healthy the pull looked |
| `out_latency_us` | the sink's estimate of pull-to-speaker delay | the DSP buffer chain plus a block of headroom. Reported to the engine on Android as the A/V compensation; recorded everywhere so the two platforms can be compared |

### Grading a run mechanically

A pass over the frame capture, which is what an automated run should assert
rather than a human watching:

1. `state` reached 3, never 6, and reached 5 if the source was finite.
2. `position_us` advanced at 1 s/s within ±2%, with **zero** backwards steps
   outside a deliberate seek.
3. `presented` total within a few frames of `decoded` total.
4. `frames_held` at the ideal value for at least the threshold share, measured
   over a window that **excludes the join** — a live join legitimately holds
   frames while the clock settles, and including it hides real regressions
   behind a known one.
5. `audio_pulled` advanced at `stream_rate` within ±1%.
6. `banked_ms` never sagged to 0 while `state` was 3.
7. With audio announced: `out_bound` non-zero, `out_consumed` advanced at
   `dsp_rate` within ±2%, and `out_peak` was non-zero at some point. Steps 1-6
   all pass on a session that makes no sound at all, so this is the step that
   catches it.

The engine capture answers the follow-up in every case: if presented lags
decoded, its drop counters say where; if the position snapped, its event log
says why.

### The smoke test runs that grading for you

`Basis > Tools > Media Player > Run Smoke Test` builds a scene around a player, plays a
source, and grades the capture against every band above. Headless, for CI or a
pre-commit check:

```
Unity -batchmode -projectPath <project> -logFile - \
      -executeMethod BasisMediaSmokeTest.RunBatch
```

Exit code 0 on a pass and 1 on a fail, with the measured numbers in the log
either way. Environment: `BASIS_SMOKE_URL` (defaults to the engine's A/V
fixture), `BASIS_SMOKE_SECONDS`, `BASIS_SMOKE_LIVE` (a live source is not
expected to end), `BASIS_SMOKE_STRICT_HOLDS`.

Frame holds are reported but **not** enforced by default, and only that band is
treated this way: an editor play session does not present on a stable display
cadence, so the share is informative there and meaningful only from a build or
a device. `BASIS_SMOKE_STRICT_HOLDS=1` promotes it to a failure for the runs
where it means something.

The scene the smoke test builds carries a real stereo output set, not a bare
AudioSource, because the audio bands grade what reached a speaker and there is
nothing to measure without one. A surround rig would leave six of its eight
outputs silent against a stereo fixture by design, so that is a listening pass
rather than a graded one.

The grading itself carries no Unity types, and is exercised against synthesised
captures — a clean run, a clock running fast, a clock that steps backwards,
audio pulled at half rate, presented trailing decoded, an error state, a run
that never ends, a capture written by a different build, an output set that
bound nothing, an output missing half its DSP blocks, outputs that ran but
mixed silence, and a video-only rig that is not expected to have outputs at
all. Each case asserts the verdict *and* the reason given, so the grader cannot
pass a run for the wrong reason.

### Keeping this accurate

These numbers are only worth having if they are true. The header line above is
quoted verbatim so a mismatch is greppable, the healthy bands come from the
measurements recorded further down this file rather than from taste, and a
change to what a column carries is a change to this table in the same commit.

Two conventions for adding to it. Name a row for what it proves rather than for
a tracker identifier — those mean nothing to a reader who has only this repo,
and one appended to a title displaces the words that would have said something.
And a row for work that has not been exercised belongs under "Not yet run"
until it has been, so that reading the matrix keeps meaning coverage.

## Rows

Grouped by pipeline area. Rows whose third column says "(in CI)" run themselves
on every commit through Gate 0; the rest are by hand, on a desk box, a device or
the test rig.

### Foundations, lifecycle and hostile input

| Row | What it proves | How to run |
| --- | --- | --- |
| Unit/property tests | Bank sizing table + properties, clock ladder (including the master-observation filter: the synthesised Quest DSP-callback jitter trace holds due-times within 3 ms filtered vs ~7 ms of episode wander raw, snaps stay on the raw error, the filter resets across generations; default-off, so the raw ladder's rows are untouched), pool leasing, ring/playhead, demux fixtures, gate blocklist, HTTP source (in-process server) | `cargo test --workspace` (in CI) |
| Session lifecycle | pause freezes position, keyframe-clean seek settles, natural end; audio-only sessions play (audio thread starts the clock, owns position) and End only once the ring's tail is consumed, and a session carrying **both** kinds of track does the same — Ended waits for the ring as well as the picture, asserted as an invariant (every frame pushed is either consumed or trimmed by the time Ended fires) rather than as a frame total, since a total cannot separate a cut tail from the serve-side lateness trim; the uncovered arm is an audio track whose **format routes but whose decoder is never built**, where the audio thread publishes 'no tail' so a refused track cannot hold a video session open for ever — no fixture pairs video with an audio codec this engine refuses, so that arm rests on the code rather than a row; seek after Ended revives the pipeline on the MP4 and HLS-TS lanes; the seek-under-drain ordering row — a seek issued from the EOS drain tail settles without a master snap and plays the remaining tail at 1x (the parked clock never restarts from a stale pre-flush frame, stale-generation work drops instead of parking, and `read_audio` serves silence while the clock is parked) | `cargo test -p media-engine --test session` (in CI) |
| ABI record padding | every record the ABI writes into a caller's buffer goes out as a whole-struct copy, which carries the struct's padding along with its fields, and padding is indeterminate — whatever the stack slot last held. On the caption path that slot is shared with the demux and caption code that has just handled stream bytes. `BmCaption`'s fields ended 268 bytes in and its leading `i64` rounded the struct to 272, so four bytes belonged to no field and crossed the boundary once per cue; they are a named field now, written as zero, and the managed declaration carries the same one so the two stay field-for-field. The other three records were already sized to avoid it. A compile-time assert on each of the four is what keeps the property when a cap or a field moves, which is the only way it comes back | `cargo build -p media-ffi` — the asserts are const-evaluated, so a record that grows padding fails the build rather than shipping (in CI) |
| Session teardown ordering | closing must not return while the session's own threads are still on its shared state. The opener is spawned before the pipeline exists and registers the demux, release, video and audio threads part-way through its own run, so a close arriving mid-open takes a handle list holding only the sampler and the opener. Joining those is not the end of the job: the threads the opener spawned meanwhile land in the list *while* that join is in progress, and a single-pass drain returns with all of them running — detached the moment the handle vector drops, still holding the shared state the caller was told is released, and past an ABI boundary where the caller's next act may be to unload the library. The drain therefore loops until the list stays empty, which terminates in a fixed number of passes rather than merely converging: the opener is the only registrar and nothing it spawns spawns further, so the pass after it is joined is the last one. The row drives the real race rather than hoping to hit it — a byte source that stalls its first read holds the opener inside its run while the close is issued, so the window is guaranteed open, and the assertion is on the handle list being empty when close returns rather than on a thread count sampled at a lucky moment | `cargo test -p media-engine --lib close_tests` (in CI) |
| Seek matrix | keyframe-clean landing across every moov layout (faststart / trailing / fragmented); HLS VOD seek-to-segment (TS + fMP4); Matroska cue seeks land keyframe-clean (targets before the first cue fall back to a linear cluster walk); raw WAV seeks land on the exact frame (PCM is linear, so no keyframe rounding); raw MP3 seeks through its Xing table and Ogg Opus by granule bisection, both approximate as they are in every player and both reporting the landing at the request; raw FLAC bisects over confirmed frame headers (or its SEEKTABLE where the file carries one) and reports the exact landing its headers state, raw ADTS estimates from its measured byte rate and rounds the landing down to a frame; raw TS and live-HLS seeks refuse as typed Unsupported; seek-to-settled measured per lane by `bench` | `cargo test -p media-demux --test mp4_stream` + `--test ts_stream` + `--test hls` + `--test raw_audio` (in CI); `bm-probe bench <lane>` |
| Conformance (ffprobe oracle) | demuxed AU stream = ffprobe's packets: announce, count, pts to 1 µs, per-packet MD5 (raw payloads, keyframes included), keyframe flags. Covers MP4 (three moov layouts) and MPEG-TS/m2ts (Annex-B video byte-exact; ADTS audio byte-exact in raw mode; LPCM announce + flow only — no canonical packetisation) | `cargo run -p bm-probe -- conformance fixtures` (in CI) |
| Fuzz | demuxers never panic/overread on hostile bytes; the HLS playlist/scheduler surface never panics on hostile playlists; the caption SEI walker + 608 decoder never panic on hostile AU bytes | `cargo +nightly fuzz run mp4_stream` / `ts_stream` / `hls_playlist` / `mkv_stream` / `flac_stream` / `mp3_stream` / `adts_stream` / `ogg_stream` / `caption_scan` (not on the MSVC host: the VPS fuzz lane — ship via `git archive HEAD \| ssh … tar -x`, nightly + cargo-fuzz installed there — or a local Linux Docker container). `fuzz/corpus/ts_stream/` seeds include the C player's four pinned crash inputs; replay with `-- -runs=0`, campaign with `-- -fork=8 -max_total_time=600` |
| Unsafe confinement | the audited `unsafe` surface is the ABI boundary, the three platform decode adapters and the present layer, and nothing else — every other crate refuses `unsafe` at compile time rather than by convention. The workspace lint set cannot carry this on its own: `unsafe_code` is allow-by-default, so the gate's `clippy -D warnings` never reaches it, and a manifest `[lints.rust]` block cannot sit alongside the inherited `[lints] workspace = true`, which leaves the crate attribute. Also that the tree's one `unsafe fn` states its preconditions as a rustdoc `# Safety` section, at the extent its callers actually check against rather than the looser one a whole-plane bound would state | `cargo build --workspace` (in CI). To see it bite, put an `unsafe` block in `media-decode/src/lib.rs` or `bm-probe/src/main.rs` and confirm that crate alone stops compiling |
| Compiler floor | the declared `rust-version` is the dependency graph's real floor rather than a guess, so resolver v3 has something to honour and a `cargo update` cannot quietly select a dependency needing a newer compiler than the build hosts carry. The floor is the graph's, not ours: edition 2024 asks 1.85 and the vendored retina 1.88, and `re_mp4` sits above both | `cargo metadata` over the graph names both the floor and who sets it — `re_mp4 0.5.1` at 1.92, with the next below it at 1.88 — and `rustup run 1.92 cargo check --workspace --all-targets` passes. Read the graph rather than an older toolchain's refusal: now that the floor is declared, 1.91 stops at the workspace's own `rust-version` and names our crates, so the refusal no longer shows which dependency set the number. By hand — neither gate installs a second toolchain, so a floor that moves is caught here rather than in CI |
| Raw-pointer obligations | a caller-supplied pointer that ends up dereferenced states its requirement in an `unsafe fn` contract rather than in a comment, so the compiler makes every call site acknowledge it. Five did not. Two were exported — `media_present::android::SessionRenderer::render`, which hands the Unity texture to `access_texture`, and `decode_mediacodec::set_java_vm`, whose stored `JavaVM*` the ceiling probe revives and dispatches JNI through — so safe code in any crate downstream could reach a vtable call on an address it chose, without writing `unsafe` anywhere. The other three are crate-internal (`VideoSink::configure`'s D3D11 device, `AsyncCodec::start`'s format and surface, `codec_name`'s codec), where the exposure is not a hostile caller but a later refactor separating the pointer from whatever keeps it alive, with nothing to flag it. Marking the function is not the whole of it: `unsafe_op_in_unsafe_fn` is `deny` workspace-wide, so each call site needs its own block, and `undocumented_unsafe_blocks` makes it carry the reason the pointer is good there. Signature-only — `unsafe` generates no code, so there is nothing here a device could show that a build does not | `cargo build --workspace` plus the aarch64 Android graph (in CI): a call site that loses its block stops compiling. The absence of further instances is a review sweep rather than a gate — every `pub fn` whose parameters name a raw pointer, checked for whether the body reads through it, discounting `extern` declarations (already unsafe to call) and accessors that only hand a pointer back |

### Demux and containers

| Row | What it proves | How to run |
| --- | --- | --- |
| TS demuxer unit rows | fixture AU/keyframe counts, mid-GOP join waits for an SPS keyframe, 33-bit PTS unwrap, m2ts stride + LPCM announce, pinned C fuzz-crash replay | `cargo test -p media-demux --test ts_stream` (in CI) |
| TS table parsing | a PSI section is walked when it arrives and when it changes, not once per packet that carries it. MPEG-TS repeats the PAT and PMT for the whole stream so a late joiner is supplied, which means an unlatched parser re-walks the same table for the life of the session and hands whatever sends it the rate. A PMT table is identified by the PID that carried it plus its `version_number` and `current_next_indicator`, and the `section_number`s walked within that table are tracked individually in a fixed 256-bit set. A section carrying that indicator clear states that it is the table due to become applicable rather than the one in force, so it is refused rather than walked: every claiming arm is guarded on the pid being unbound, and a next table read first takes a binding the applicable one can no longer replace. Every part of that is load-bearing: a PMT may legitimately span several sections arriving in any order, so remembering only the previous one lets an alternating pair thrash the latch forever; leaving the PID out of the identity makes a PAT that re-points at another PMT PID collide with the table it replaced (both are normally version 0, section 0) and get skipped, stranding its tracks unbound; and a section is marked walked only *after* it is known to be a `program_map_section` in PSI long form whose stated length reaches its own entries, so a malformed copy — walked over without reading anything — cannot consume the identity a sound copy arriving later needs. That proof runs over the entries as well as the header: an `ES_info_length` reaching past the CRC is walked as far as it goes and then abandoned, so taking the identity on the way stranded the program for as long as that version stood — one malformed copy, which a lossy transport reaches by accident and a hostile stream reaches on purpose with a single packet. What the proof applies to is decided by the stated length against the packet: where the whole section is here the entries must tile the space between the descriptors and the CRC exactly, and a remainder too short to hold another entry is the same defect as one running past it. A section longer than its packet is exempt, because what lies past the clamp is missing rather than wrong — proving it would refuse every PMT too large for one packet and bind none of the tracks its first packet does name. The PAT records which program it selected as well as which pid, and a section for any other program on that pid is screened out at the header: a PMT pid may legally carry more than one program's tables, told apart only by the section header's program_number, and without that the first section to arrive bound its elementary streams and latched the identity while the selected program's — same version, same section number — was dropped as a repeat of it. Screened rather than folded into the section identity, because widening the identity does not choose between two programs (the claiming arms are guarded on the pid being unbound, so arrival order still decides) and would let a pair from two programs replace each other in the single-table latch on every cycle. Both table parsers also run only on a payload-unit-start packet, since a continuation payload carries no pointer field and reading one takes its media bytes for a table header. Both screen the same two header fields, too: the PMT's checks went in without the PAT's, leaving anything on PID 0 able to repoint the PMT PID, including a copy stating it becomes applicable later. That is worse there than it is on the PMT PID, because the PID is the table's identity — repointing it stops the demuxer reading the PID in force and discards the sections already walked under the old one. Rows over synthetic single-packet tables: a *repeated* section at the same version is not walked again (observed through the unclaimed-stream_type notes, which is the walk's only outward effect — the pid bindings are idempotent), a version bump is walked, a program that gains its tracks in the bumped table still binds them so the latch costs nothing real, a repeated two-section cycle walks each section exactly once, a PAT re-point to a new PMT PID at the same section key still binds that table's tracks, a short section followed by a sound one at the same identity still binds, a not-yet-applicable table binds nothing while the applicable copy behind it binds its own pids rather than merely binding something, an entry whose descriptor block runs past the CRC leaves the identity to the sound copy behind it, a tail two bytes short of the CRC does the same, a section stating more length than its packet holds still binds the tracks that packet names, a non-start payload holding a well-formed section is not read as one, a not-yet-applicable PAT naming a decoy PID leaves the tracks bound from the PID in force, a section on PID 0 carrying a PMT's table_id does not take the PAT's place, a PMT for another program sharing the selected pid does not bind ahead of the selected program's own, and a whole section that fails its own CRC leaves the walk to the sound copy behind it. That last one is the check the structural ones cannot make: they say a section is shaped like a table, not that it arrived intact, and a corrupted copy satisfying them binds a PID that stands for as long as its version does — one bit flipped in an entry binds pid 513 where the sound copy names 512, and takes the identity the sound copy needed. Both parsers run the CRC-32/MPEG-2 over a section that arrived whole; a clamped one is exempt for the same reason it is exempt from the tiling proof, its CRC being in the packets behind it. The synthetic tables stamp real CRCs now and every helper that edits one restamps, or the row it feeds would be refused for its check rather than for the shape it was built to have — the ragged-tail helper is the worked example, since it states a longer section than it built and the two bytes the demuxer then reads from the packet's filler have to be carried into the payload before the stamp, or that row silently becomes a second copy of the CRC one and leaves the remainder check uncovered. Each such row is checked in both directions: it still passes with the CRC guard removed, and goes red with its own screen removed; the polynomial is pinned against ffmpeg's own tables by the two rows that demux the committed TS and m2ts fixtures. **Residual, deliberate:** a stream that bumps the version on every table still gets a walk each time, which is the correct response to a table saying it changed; what bounds that is the note cap, since the walk itself is then a few dozen integer operations with no allocation. A section spanning packets is still clamped to what the packet holds rather than refused, so a PMT too large for one packet binds the tracks its first packet names — refusing it instead would bind none. The fixtures are the guard on the whole change: all four TS/m2ts conformance lanes stay byte-exact against ffprobe, and the pinned C fuzz-crash inputs still walk to EOS | `cargo test -p media-demux --test ts_stream` (in CI); `cargo run -p bm-probe -- conformance fixtures` |
| Demux note caps | diagnostic notes are drained once, after open, so nothing empties them again for the rest of a session — which makes any note whose text comes from the stream rather than from a container's fixed structure an unbounded sink on the demux thread. Two of them were: the TS unclaimed-`stream_type` note, where every entry a PMT lists is unclaimed once video and audio are bound and the distinct `(stream_type, pid)` space is 2²¹, with a linear duplicate scan over everything held so far; and the HLS scheduler's window-jump and skipped-segment notes, one per event for as long as a live lane runs. Both go through one capped, deduplicating helper: at 64 the collection stops accepting, and the note is not even formatted, so a source generating them endlessly costs nothing and the duplicate scan is constant-cost rather than quadratic in what was sent. Rows cover the helper's own contract (fills and stops, does not build a note it has no room for, duplicates recorded once) and each sink through its real path: a TS stream offering 620 distinct unclaimed entries across bumped PMT versions, asserted at the cap and asserted again at five times the input so the row pins boundedness rather than smallness, and a live playlist whose window jumps 39 times with nothing fetchable. Note text after the open-time drain reaches no diagnostic either way; the cap bounds the collection, it does not change what is reported. The container demuxers are inside the same bound now. They were left out of it on the reasoning that a note whose text comes from a container's fixed structure has a finite number of instances, which holds for a layout and not for a track list: a file states how many tracks it has, every track the demuxers cannot use is a note, and several of those notes carry a codec id straight out of the file. Eighteen sites across Matroska, MP4, Ogg and the HLS master-playlist pick — more than the track arms alone, because several are written across two lines and a single-line search for the call reports them clean. The row builds a Matroska naming 256 audio tracks the demuxers can make nothing of behind one playable video track: video for the playable one because a file with nothing usable in it is refused outright and hands back no notes to count, while an audio track that binds sends every later audio track to the catch-all arm rather than the one that names the codec id | `cargo test -p media-demux --lib demuxer` + `--test ts_stream` + `--test mkv_stream` + `cargo test -p media-hls --test hls` (in CI) |
| Matroska demuxer rows | pinned AU/keyframe counts on the remuxed A/V fixture, stored-H.264 → Annex-B with SPS/PPS on keyframes, VP9/Opus WebM announces with frames flowing, CodecDelay subtracted from audio pts (Opus pre-skip as negative lead-in), cue seeks landing keyframe-clean at or before the target (vendored matroska-demuxer, see third_party/matroska-demuxer/PATCHES.md) | `cargo test -p media-demux --test mkv_stream` (in CI) |
| Raw audio demuxer rows | sniffed routing for FLAC/Ogg/MP3/ADTS/WAV heads (+ ID3v2 → MP3); pinned AU counts against ffprobe's packet view on the sine fixtures; exact sample-derived pts; the Xing/Info frame consumed as metadata; ADTS header strip + 2-byte ASC reconstruction with the 1..=6 channel screen; Ogg Opus pre-skip as negative pts; MP3 parses its Xing/Info or VBRI frame for the length and seek table and Ogg Opus reads the tail page's granule for its own, so both report a duration and seek; FLAC takes its length from STREAMINFO and bisects for a position, landing on the sample number its frame headers state (the committed fixtures carry no SEEKTABLE, so the tests are the bisection arm, on frames from ~1.3 kB to ~52 kB); ADTS states nothing anywhere, so its length and its landing both come from the byte rate averaged over the leading frames, with the landing rounded down to a frame and the walk restarted on a confirmed header; WAV sniffs on RIFF/WAVE, reads WAVE_FORMAT_EXTENSIBLE through its SubFormat GUID and valid-bits field, serves every data byte once in order in ~20 ms whole-frame AUs, treats a stated data size of 0/0xFFFFFFFF as unknown and stops at EOF, caps the chunk walk, refuses non-integer PCM typed, and seeks to the exact frame; the 7.1 lane: 8-channel FLAC demuxes and announces 8 channels, ADTS with channel_configuration 7 refuses typed at open; a FLAC that states a SEEKTABLE lands by it rather than bisecting, reaching the same frame for fewer reads (the table is spliced in by the test, since nothing available writes one); embedded cover art surfaces from FLAC's PICTURE block, MP3's ID3v2 APIC and MP4's covr atom, and a file without one reports none | `cargo test -p media-demux --test raw_audio` + `cargo test -p media-demux --lib` (in CI) |
| Embedded cover art | the picture every audio container can carry, extracted and never decoded: FLAC's `PICTURE` block, Ogg's base64 `METADATA_BLOCK_PICTURE` (the same structure, so one parser serves both), ID3v2's `APIC` across v2.2/v2.3/v2.4, and MP4's `moov/udta/meta/ilst/covr` (read from what the box parser already walks, with the format sniffed from the magic since `covr` states only "image"). The bytes cross the ABI compressed and Unity decodes them, so no image parser sits in the untrusted path. Unit rows pin the shapes that bite — v2.3's plain frame size read as v2.4's syncsafe one walks off the end of the frame, a UTF-16 description terminates on a null pair not a lone zero, an unsynchronised tag yields no art rather than a corrupt picture, and hostile lengths are refused not allocated. Where a file holds several pictures the front cover wins; the cap is 16 MB | `cargo test -p media-demux --lib artwork` + `--test raw_audio` (in CI). By hand: a source with no video puts its art on the output texture — use a **deliberately asymmetric** picture, since a symmetric one hides an inverted frame (that is how the sink flip was caught) |
| Matroska stated geometry | the container states its sampling frequency and its duration as EBML *floats*, and the walker below rejects only negatives — `NaN < 0.0` and `INFINITY < 0.0` are both false — so the casts that convert them saturate rather than failing, silently and in either direction. Every number a Matroska file states about its audio is therefore an allocation input the file chooses: the announced rate is what sizes the playback ring, so an unbounded one lands a multi-gigabyte request off a one-field edit. Stated channels are the same shape a level down, narrowed from a u64. Rows patch the declared bytes of the real fixture and assert on what comes out: a NaN, an infinity and a merely enormous rate each skip the audio track with a note while the video still announces and plays, the skipped track is absent from the audio picker too (so the offered list and the bound track cannot disagree), and a near-integer rate rounds to the rate the encoder meant rather than to the one below it (the field is a float because the /1.001 pulldown family genuinely is not integral, so refusing non-integral values would refuse real files), and a hostile duration is reported as *no* duration rather than as a fabricated one — a saturated zero reads as live to every "duration <= 0" test and a saturated maximum is a 292 000-year VOD. The unpatched fixture still reports its real duration, which is what keeps the guard from being a refusal of everything | `cargo test -p media-demux --test mkv_stream` (in CI) |
| MKV/WebM playback | EBML-sniffed routing; H.264+AAC in MKV plays end to end; VP9/Opus WebM plays both tracks; AV1/Opus WebM plays both; H.265+AAC in MKV plays through the hardware route (hvcC parsed, VPS/SPS/PPS converted to Annex-B — HEVC has no software rung, so machines without the DXVA profile refuse it typed) | `cargo run -p bm-probe -- play fixtures/mkv/h264-aac.mkv --duration 8`, `…/mkv/vp9-opus.webm`, `…/mkv/av1-opus.webm`, `…/mkv/h265-aac.mkv`; mr.town `/vod/` codec-batch lanes |
| Audio track selection | multi-audio MP4 and Matroska offer every audio track in container order with the language the container states (and, for Matroska, the track name); a single-track source offers nothing, so a picker hides itself on an empty list; the requested index binds that track, and an out-of-range index falls back to the first with a note rather than failing the session. Verified end to end on a fixture carrying 440 Hz on track 0 and 880 Hz on track 1: `--audio-track 0` decodes 440 Hz and `--audio-track 1` decodes 880 Hz, measured by zero crossings over the pulled PCM | `cargo test -p media-demux --test audio_tracks` (in CI); `cargo run -p bm-probe -- play fixtures/h264-multiaudio.mp4 --audio-track {0,1} --audio-out out.f32` |
| Audio tracks with no metadata (the recording shape) | a capture that puts the mix, the microphone and the desktop on separate tracks states neither a language nor a name, so enumeration must not depend on either and the labelling above it must not rely on language to tell rows apart. Pinned on a three-track fixture with no tags: all three are offered, each index binds its own track, and the managed label falls back to the 1-based position so the rows stay distinguishable. Matroska carries OBS's track names; MP4 does not, which is the case that would otherwise render three identical rows | `cargo test -p media-demux --test audio_tracks untagged` (in CI) |
| HLS scheduler unit rows | playlist parse + refusals (EXT-X-KEY, BYTERANGE, I-frame-only), EXT-X-MAP carry-forward, live window advance/refresh cadence, window fall-out jump + discontinuity, stated-splice TS rebuild with trailing-PES flush, master variant choice, live join point, VOD seek-to-segment — all over a virtual fetcher, no sleeps | `cargo test -p media-demux --test hls` (in CI) |
| CEA-608 caption lane | in-band captions (ATSC A/53 SEI in the H.264 AUs) decode through the media-bitstream SEI walker + 608 field-1 (CC1) state machine on the demux thread and surface as cues on arrival with their due PTS (managed side displays at position): the authored fixture's scripted sequence — pop-on, special + extended characters, EDM clear, two-row roll-up with CR — arrives complete with 2 s video-timeline spacing; unit rows cover the decoder's modes, doubling dedup, channel-2 rejection, the backwards-PTS epoch reset (clear cue emitted) and hostile SEI length prefixes; seeks reset the decoder and clear the display | `cargo test -p media-bitstream` + `cargo test -p media-engine --test session caption` (in CI); by hand: `bm-probe play fixtures/h264-608-640x360-30fps.ts --duration 10` prints the cue timeline; regenerate the fixture with `python tools/gen-caption-fixture.py` (gated on ffmpeg's own subcc decoder as oracle) |
| SEI user-data lane | opaque application data stamped into the video as SEI `user_data_unregistered` (payload type 5, H.264 and H.265) is surfaced on the demux thread from the same AU scan as captions — the 16-byte UUID split off, the bytes after it left unparsed, each message stamped with its AU's PTS — and handed over in decode order, lossless while the stream stays inside the ring's bounds (beyond them it drops oldest): the authored fixture sits well inside them, stamps one message into every one of its 180 AUs, and every one arrives, in order, with its filler intact and its PTS stepping one frame, while x264's own build-string message rides through on the first AU under its own UUID (so a consumer filters on the UUID, and the engine carries no application's identity). Unit rows cover the UUID split, short and foreign messages, a 10 KiB payload behind a long length prefix, the HEVC NAL layout and the backwards-PTS epoch (including at the `i64` extremes, where the comparison must not overflow); a new epoch clears the ring before the restarting AU's own messages go in; the ring's two bounds (1024 messages, 16 MiB) both drop oldest, a message over the 64 KiB per-message ceiling is refused, and a drain takes only whole messages that fit its byte budget while a head message that could never fit is dropped rather than left blocking. Seeks clear the ring; the managed side holds each message until the position reaches its PTS and delivers it through `BasisMediaPlayer.UserDataReceived` | `cargo test -p media-bitstream` + `cargo test -p media-engine --lib user_data_ring` + `cargo test -p media-engine --test session user_data` (in CI); by hand: `bm-probe play fixtures/h264-sei-userdata-640x360-30fps.ts --duration 10` prints a per-UUID message and byte count; regenerate the fixture with `python tools/gen-sei-userdata-fixture.py` |
| SEI user data — managed delivery at the playhead | `BasisMediaPlayer.UserDataReceived` fires once per message when the playback position reaches its PTS, in PTS order, with the UUID as a `Guid` whose text form matches the wire bytes and the payload borrowed for the call; messages are held until due whether or not anyone is subscribed, so a subscriber arriving mid-session receives everything still to come and nothing already past. The last clause is the one that failed first: an on-demand open banks the whole pre-roll before the first frame shows, the first drain takes a tick's worth of it (30 records at 2 KiB against the 64 KiB byte budget), and a subscriber one frame behind the player's tick lost exactly those 30 while the drain discarded what nobody was listening for. It holds them now. Exercised end to end by the VRSL-URP package's `Towneh.VRSL.URP.Basis.Tests` rows, which play `https://mr.town/vod/truss-dmx-ramp.ts` (300 frames, one Truss DMX record each) through a real player in a PlayMode rig, consume through `UserDataReceived`, and read the lighting channel buffer back through the shader: 300 of 300 delivered in order, the buffer byte-exact against the expected pattern at two universe counts, and on the damaged sibling 30 of 300 refused by CRC with the rest still exact | vrsl-urp `.compilecheck/tests.sh` (runs both test assemblies), or the Test Runner window on assembly `Towneh.VRSL.URP.Basis.Tests`; `VRSL_TRUSS_FIXTURES` points the rows at a local copy of the fixtures (`BasisApps/basis-truss-dmx-fixture/`) instead of the hosted one |

### Decode

| Row | What it proves | How to run |
| --- | --- | --- |
| Software decode adapters | claxon FLAC decodes the demuxed fixture to exactly 6 s of PCM with frame-accurate pts, and the 7.1 fixture to eight-channel PCM (claxon's cap is 8); libopus Opus decodes all packets with pre-skip before the origin; surround Opus (mapping family ≠ 0) and broken headers refuse typed; rav1d decodes the AV1 WebM fixture completely with monotonic pts | `cargo test -p decode-sw` (in CI) |
| PCM adapter | the one integer-PCM path serves both lanes: 16- and 24-bit widen to the ring's floats identically from little-endian (RIFF/WAVE) and big-endian (Blu-ray HDMV LPCM) bytes, with 24-bit sign extension pinned at full-scale negative; Blu-ray assignments 9/10/11 move the LFE off the end of the frame into its WAVE slot while assignment 0 passes straight through; a bits code, channel count or remap width the adapter cannot serve refuses typed; a torn tail frame yields no chunk | `cargo test -p decode-sw` (in CI) |
| MF adapter contracts | H.264 + AAC through the real in-box MFTs (priming pts, output ranking); MP3 through the in-box decoder on the same shared sync driver; VP9/AV1 through the Store-extension MFTs found by probe (rows skip loudly if an extension is absent — that absence is the diagnostic the engine reports) | `cargo test -p decode-mf` (in CI, Windows) |
| Strided plane copies | the row stride a decoder reports is signed at every source it comes from, and was trusted at every site it was used. Media Foundation states it twice — `IMF2DBuffer::Lock2D`'s out-parameter is a LONG, and `MF_MT_DEFAULT_STRIDE` sits in a UINT32 attribute but is defined as one — and documents both as possibly negative, meaning a bottom-up surface whose first scanline is the last row. Read unsigned, −1920 becomes an offset near the top of the address space; through the default-stride path it also won the `.max()` against the frame width, so the clamp that was there to catch an under-reported stride selected the bad value instead. rav1d's `Dav1dPicture::stride` is the same shape, a signed `ptrdiff_t` whose negative case the crate documents. Now a stride the forward row copy cannot follow refuses typed rather than being cast; a stride shorter than the row it carries refuses where the buffer states its own pitch, and is still clamped to the row width where the value is only the media type's hint, which is what that path always did; and where the buffer will say how much it actually mapped — `Lock2DSize` taken in preference to `Lock2D`, the decoded length on the plain lock, checked against the capacity it reports alongside it — the copy is checked against that rather than against an assumed geometry — and a buffer that states a mapping its own first scanline lies outside has contradicted itself, which refuses rather than falling back to the stride check — measured unsigned, since a signed difference of two addresses overflows for a mapping stated in the other half of the address space, making the most incoherent input the one that panicked a debug build instead of being refused. Alongside those, a frame size NV12 cannot represent refuses at the decoder: an AV1 frame size is `frame_width_minus_1 + 1` so either axis may be odd, and NV12's chroma is exactly half the luma in each axis, so the interleave would drop the last column and the bottom row and the present layer would then refuse the frame once per frame instead of once. Pinned as unit rows over the checks themselves, because neither a live MFT nor rav1d will produce a bottom-up surface, an incoherent mapping or an odd frame on request — encoders will not emit odd 4:2:0 dimensions either, which is why the last of those has no fixture and is a contract guard rather than a row. Two bounds sit either side of that copy and are rows here as well. The destination is allocated from the decoder's stated frame size before either lock has reported anything, so a product that wraps allocates short and the copy then writes the geometry it was given past the end of it — the 2D path is where that lands, since a buffer without `IMF2DBuffer2` states no mapping for the source check to test against. The two planes are computed with checked arithmetic now and an overflowing frame size refuses. And the plain lock reports both the capacity the buffer was created with and the length of the data actually in it; the copy was bounded by the capacity, which is the larger of the two and covers bytes the sample never wrote, so it is bounded by the decoded length instead, with a buffer claiming more valid data than it maps refused rather than believed. That the tighter bound refuses nothing real was measured through the software route with `BASIS_MEDIA_DISABLE_HW_DECODE` set: H.264 640x360 decodes 180 frames and VP9 90, both matching the run before the change. The software AV1 route's destination is computed the same way, which makes the last unchecked product in the tree checked and its unsafe block's claim to have sized the destination true; both axes are even by the odd-geometry refusal above it, so the packed length is exact. No shipped target reaches that wrap — every one is 64-bit and rav1d's dimensions come from an i32 — so it is a contract guard beside the odd-frame one rather than a reachable defect. Both routes take that sizing from one helper beside `Nv12Frame` rather than carrying the arithmetic each, so the packed layout has a single rule — including the refusal of odd dimensions, which the software AV1 route made on its own while the Media Foundation ones did not. Rounding an odd axis down returns a length that fits a copy running half-height rows and drops the bottom row of the picture; rounding up returns one a caller overruns. The helper is public, so neither is a size it will answer with, and rows pin the refusal on either axis and on both. The DXVA readback bounds its source as well: the packed length says where the copy writes, while a mapping whose pitch is narrower than the frame row or whose texture is shorter than the frame puts both loops outside what was mapped, and the mapped pointer itself went unchecked — the same shape the software route checks before its own copy, refused now with the texture unmapped first. It also computes its length before it maps the staging texture: a fallible step between `Map` and `Unmap` returns with the texture still mapped and it is then dropped that way, which is the discipline the lock paths keep and the readback did not | `cargo test -p decode-mf --lib` + `cargo test -p decode-sw --lib` (in CI). That legitimate strides still pass is what the real-decoder rows around this one prove: the hardware-vs-software byte match and the fixture lanes all run through the checked path |
| Windows hardware decode | the DXVA route: sync MFTs bound to a D3D11 device through the DXGI device manager, decoder-allocated NV12 texture-array slices flowing as opaque frames into the conversion pass (one GPU subresource copy, no CPU touch), presenter sharing the decode device. Conformance: hardware output byte-matches the software route over the visible frame for H.264 (including after a flush/restart), VP9 and AV1-vs-rav1d — the coded pad rows (360→368) are unspecified content and excluded; HEVC (no software oracle) pins count/dims/monotonic-pts. Ported C contracts pinned: sample released on every path (RAII payload), per-iteration `GetOutputStreamInfo`, subresource index honoured (the present-slice array test drives slice 1 before slice 0), aperture re-read at stream change, sizeless-HEVC refusal before the MFT is configured, AV1 config OBUs riding the first accepted AU, post-reset output floor | `cargo test -p decode-mf --test dxva_decode` + `cargo test -p media-present --test gpu_pass present_slice` (in CI; rows skip loudly where the GPU lacks a profile); integrated: every Windows session/smoke row now rides the hardware route where the GPU has one |
| Forced hardware fallback | with `BASIS_MEDIA_DISABLE_HW_DECODE` set, every hardware probe reports absent: the default preference lands on the software rung with a `DecodeFallbackHwToSw` diagnostic and plays to a natural end; `hardware_only` instead refuses typed (CodecRefused — video mutes, audio owns Ended). The same env var is the field escape hatch for broken drivers. The runtime DXGI-backing signal (an output sample without GPU backing = the MFT silently fell back to CPU) reroutes mid-stream through the same ladder | `cargo test -p media-engine --test hw_fallback` (in CI; its own binary — the env var is process-wide) |
| Decode preference | descriptor `decode_preference` (`hardware_with_fallback` default / `hardware_only` / `software_only`) → route ladder; a rung the platform lacks refuses typed (Android has no software rung; headless has no hardware rung); managed `BasisMediaPlayer.DecodePreference` static (never a serialised inspector field) appends it at open. `software_only` is the §11 CPU A/B lever | the hw_fallback rows above cover both non-default rungs; by hand: `bm-probe` lanes stay green either way once bm-probe grows a flag (engine-side the descriptor field is the surface) |
| Software-route cap | software decode routes accept up to 1080p60 coded pixel rate (1920×1088×60; dimensions-only gate ≤1920×1088 while no demuxer states fps) and refuse above it in the CodecRefused posture before any decoder builds — on the direct `software_only` route and the fallback rung alike; software capability entries state the same ceiling the engine enforces. Tightens on field evidence (a one-constant change in `media-engine/src/route.rs`) | `cargo test -p media-engine --lib route` (in CI: the pixel-rate/dims policy rows + the over-cap refusal through the real Windows ladder) |
| Capability contract | the §6.11 engine-declared set: the JSON wire shape pinned byte-exact (versioned contract, field renames are breaking), the built set matching what this build actually routes (software rungs: H.264 + rav1d AV1 constant, VP9 present exactly when the Store-extension probe finds a decoder, every software entry stating the enforced ceiling 1920/1088/60; hardware entries present exactly where the two-leg DXVA probe passes, with the resolution-ladder ceiling; the audio adapters' real channel screens, `rist` transport present iff the feature compiled in, one `pcm` entry at 8 channels covering both RIFF/WAVE and the LPCM carried in MPEG-TS), and `bm_capabilities`' size/fill calling convention over the ABI (short buffer untouched, length still returned) | `cargo test -p media-engine --test capabilities` + `cargo test -p media-ffi --test capabilities` (in CI, Windows); by hand: `cargo run -p bm-probe -- caps` prints the blob (`--compact` for the exact ABI bytes; add `--features rist` to see the rist row) |

### Present, audio output and clock

| Row | What it proves | How to run |
| --- | --- | --- |
| Clock correction law | the master correction is proportional with the cap as a ceiling, not a fixed rate at the cap. Pinned: a join-sized 690 ms error converges in ~4.9 s against the ~34.5 s the 2% cap alone needs, and a 100 ms error — the size a correctly anchored origin should leave — converges inside 1 s; the rate strictly falls while non-zero as the error closes, which a fixed-rate law never does (it runs at the cap until the dead band, then drops to zero, and that is what produced the overshoot burst on live joins); the instantaneous rate stays inside whichever ceiling is in force, wide for 1.2 s after a snap or a master adoption and 2% from then on; and the ceiling is treated as caller-supplied data rather than a promise — `ClockConfig`'s fields are public, so a negative ceiling (which would panic the clamp on `min > max`) and one at or beyond 1x (which could stop or reverse position) are both bounded at the point of use, driven from both signs of error; and an expired fast window is closed without waiting for the next observation, since the rate persists between them and a master going quiet just after a join would otherwise keep the wide ceiling running indefinitely. The residual on a 690 ms error is the *join error’s size*, not the corrector — the presentation origin is what makes it that big, and sizing the fast window to swallow it would be tuning the controller around a defect | `cargo test -p media-clock` (in CI) |
| GPU conversion pass | the §6.8 D3D11 NV12→BGRA pass agrees with the CPU reference converter (same maths, point-sampled chroma) across every stated matrix/range on synthetic sweeps and on decoded fixture frames through the full shared-texture handoff; the reference itself agrees with the integer maths the CPU path shipped with | `cargo test -p media-present --test gpu_pass` (in CI, Windows) |
| Shared-texture handle lifetime | `CreateSharedHandle` hands back an owned NT handle, and holding it keeps a reference to the texture — so leaking one strands the BGRA surface's video memory as well as a kernel handle, per presenter build, and the presenter is rebuilt on every announced coded-size or codec change. A playlist that alternates rendition resolutions across discontinuities drives that at segment cadence with nothing bounding it. The presenter closes the handle now — in `Drop`, and on the two fallible steps between the handle's creation and the presenter existing, since `Drop` cannot run before that and each of those returned leaving it open. The row pins the object rather than the value: a duplicate holds the kernel object alive across the drop, so a handle value the OS recycles immediately afterwards still reads as "no longer the shared texture" instead of as a leak. Closing it is what made the consumer's staleness matter — it opened once per texture registration and then kept copying out of the texture the presenter had stopped writing to after a rebuild — so the consumer carries the handle it settled against and reopens when the engine publishes a different one. A failed open is no longer terminal for the session: it was cached against the handle, and only a fresh handle cleared it, which the engine publishes only when it rebuilds the presenter — so an open that lost the race the close above created, or ran before the device was ready, left a session that never presented again. Attempts are counted and up to eight are made for one handle, which keeps what the cache was for: a handle that is genuinely dead stops calling open once per render event. A unit row drives the state machine, since the transitions are the whole of the change and the D3D11 open around them needs a device | `cargo test -p media-present --test gpu_pass dropping_the_presenter` (in CI, Windows). Nor do those two build paths: provoking a keyed-mutex cast or a convert-pass build to fail needs fault injection the type does not offer, so what stands there is that they are the only fallible steps between the two points and both now close it. The reopen half has no automated row: it needs a source that changes coded size mid-session, so it is by hand on an HLS ladder whose renditions differ in resolution across a discontinuity — the picture must stay live across the change rather than freezing on the last frame of the old size |
| Multichannel interleave order | the PCM ring's multichannel interleave is WAV/channel-mask order (FL FR C LFE BL BR) — the order every decoder route emits (MF's PCM convention, the Android AAC decoder's FDK default, FLAC's stored order) and the order the managed stereo downmix keys its ITU-coefficient matrix on: a 5.1 channel-marker fixture (one distinct sine per speaker) plays through the full session and each interleave slot carries its WAV-order tone | `cargo test -p media-engine --test session multichannel_interleave` (in CI, Windows); the fixture is `fixtures/sine-48k-51.m4a` |
| PTS-annotated ring serve | the media timeline, not the sample count, masters the clock: the ring carries chunk pts markers, the playhead interpolates them, and the serve trims a head running >300 ms late against the session clock in bounded ≤1024-frame steps (narrated via the rate-limited AudioTrim event, with the cumulative total carried by the capture's `audio_trimmed_frames` column). Pinned: the ms-quantised passthrough pattern (1024-sample chunks, ~1010 samples of pts) keeps the playhead on the pts timeline; a chronically late head trims bounded and every pushed frame is served or trimmed; a completely full ring on an honest timeline (the VOD startup-burst shape) never trims | `cargo test -p media-engine --lib audio` (in CI); live repro: `bm-probe play rtspt://mr.town:8090/imax51 --duration 75` — master honest (the only snap is the loop-wrap splice), trims ≈ the lane's ~1.6% structural surplus, stereo twin zero trims |
| Audio ring generation swap | rebuilding the ring for a new generation — a seek's flush, or a format change a live lane can announce at whatever cadence it likes — resets the shared block, and that reset and the consumer slot's swap are one critical section. The pull path holds the slot for its whole duration, so a pull already in flight would otherwise put the retired generation's playhead, consumed count and pull wall back on top of the reset and undo it silently; what survives is the previous timeline's absolute position measured against the new one, which is the whole seek or format-change distance, and the clock masters on that playhead — so the ladder answers with a snap to a position nothing has decoded behind and the pool grades every frame after it late. The ring goes in through `install_audio_generation`, which takes the slot by reference, so there is no way to ask for the reset without it. The row drives the interleaving rather than waiting for it: it holds the slot the way a pull in flight does, starts the swap on another thread, pulls, and only then releases. The producer and the consumer are both retired with the decoder they fed: a refused decoder used to leave the producer from the format before it in place, and since the flush arm installs a fresh ring only where there is a decoder to size it from, that one survived a seek and the end-of-stream check then read drain state off a ring that no longer matched the timeline. The consumer half goes the same way, in the refusal arm as well as the flush arm: left installed it serves the retired format's tail and keeps writing a playhead for a muted lane, while the drain check — seeing no producer — reads the lane as finished, so between them they declare an end over samples the pull path can still reach | `cargo test -p media-engine --lib audio` (in CI) |
| Audio pts-marker budget | chunk cadence belongs to the stream and the marker ring is fixed at 1024, so a source muxing minimum-duration access units — FLAC blocks go down to sixteen samples — offers more chunks per ring fill than there are slots. A lost marker leaves the consumer extrapolating an older chunk's timeline over the ones that followed, which is what the serve-side lateness trim measures against, so it takes audio in either direction and raises nothing. A chunk contiguous with the marker the consumer already holds carries nothing it cannot interpolate and no longer takes a slot, so the budget counts discontinuities in flight rather than chunks; a marker that is needed and cannot be written is back-pressure, the same answer a full sample ring gives. Both halves are pinned: a gapped stream of sixteen-frame chunks keeps the playhead on every chunk's own pts, and the same stream contiguous still fills the ring to its two seconds instead of stopping at the marker budget | `cargo test -p media-engine --lib audio` (in CI) |
| Audio ring sizing | the ring's two sizing factors — sample rate and channel count — arrive announced, by a container or by a platform decoder's reported output format, so neither can be trusted to name an allocation. The frame count was a plain u32 multiply, which a rate near the type's maximum wraps; release builds run without overflow checks, so the wrap is silent and the capacity lands in the billions, a request whose failure aborts the process rather than the session. Saturating arithmetic and a ceiling on the result, so no announced pair can size it past 6 Mi *samples* — two seconds of 384 kHz 7.1, around 24 MiB once each sample is an f32. That ceiling counts samples while both ends of the ring move whole frames, so it is rounded down to one: a channel count that does not divide it would otherwise leave slots no push or pull can reach. A geometry too wide for a single frame keeps the flat bound instead, since rounding that down lands on nothing and a ring nothing can push into is the worse answer. The announced values still reach the shared state verbatim: this bounds the allocation and deliberately not the timeline, which the playhead reads as stated. One case has no working answer rather than a bounded one — a geometry whose single frame is wider than the cap holds no frame under either branch, so every push is refused and the lane is inert. The flat cap keeps it bounded, which is what the hostile-geometry row pins, and it now says so rather than reading as a stall. No decoder can produce it: the ring is sized from what one reports and every adapter screens its channel count far below the cap | `cargo test -p media-engine --lib audio` (in CI) |
| A/V output-latency compensation | the managed sink's reported output latency shifts the audio master back so video paces to the *audible* position: the playhead subtracts the stored figure exactly; a mid-play 100 ms report engages the slew rung (never the snap) and the clock settles onto the compensated master within the dead band; the ABI setter clamps to 0..=500 ms; default 0 leaves every existing row byte-identical (the managed side sends it on Android only) | `cargo test -p media-engine --lib audio` + `--test session audio_latency` (in CI); on device: the fixture package pass + the rtspt stereo lane stay green, the sync itself is a listening judgement |
| Render-event frame selection | presentation due-ness is decided in the Unity render event at display cadence, with one vsync of lookahead against a lock-free clock mirror, so the selection and display quantisers are the same clock: a synthesised 24 fps grid selected by 72 Hz events presents every frame exactly once at the ideal 3-event hold across a full phase sweep, with ±1.5 ms event jitter never producing the late/early pair signature; a parked clock (startup, seeks, pause) selects nothing; the interval EMA tracks cadence and rejects hitches; the 500 ms consumer-liveness window hands selection between the render event and the video thread's tick-paced fallback (headless sessions never see an event, so every headless row runs the unchanged fallback path) | `cargo test -p media-engine --lib present` (in CI); integrated: the Unity host autotest (the render-event mode on Windows); on device: the Steady-lane cadence row's hold-interval histogram |
| Output-texture ownership | the component installs and disposes of its output texture through one helper, so every path that ends a registration hands the texture on rather than dropping the reference. What that means differs by platform, and the split is the plugin's: on Vulkan it holds an image view it can only destroy from a later render event, so the texture goes to the retirement queue and is released once those events have been issued; everywhere else the plugin owns nothing that outlives the session, so the texture is destroyed at once and Unity releases the graphics resource through the render command queue, after the events already issued for it. Before this the reference was simply dropped, which left one texture of full video size stranded per open/close cycle on the desktop path — and `Open` closes first, so `ReopenAtPosition` and a track switch each cost one too. The cover art is excluded by name: it has an owner already, and it is the one texture here the session never drew into. The replace arm cannot be reached today, since the texture is only created where there is none, so what a row can show is the close arm | editor or a standalone build: open a source, close it and re-open, half a dozen times, with the Profiler's Memory module on Texture2D count — it should return to where it started rather than climbing by one per cycle. The Vulkan arm rides the device row above |
| Headless audio lane | decoded PCM as raw interleaved f32 | `bm-probe play <src> --audio-out out.f32`; inspect with `ffplay -f f32le -ar 48000 -ch_layout stereo out.f32` |

### Buffering, pacing and resilience

| Row | What it proves | How to run |
| --- | --- | --- |
| Live join starts on audio | presentation does not wait for the picture: every live session is audio-leading, so the clock starts at the first banked PCM and video appears at its keyframe against it. This replaced a preroll wait that held the clock until *both* legs had content — a video-led start began at a media time whose audio had not arrived (~0.78 s behind on the live 5.1 lane), so every chunk was late on landing, the serve trim discarded it and the clock took a seconds-long snap once the master appeared. Starting on audio removes the cause rather than waiting it out, so the wait, its 2 s cap and the pre-join shed are all gone. **Measured on `rtspt://mr.town:8090/imax51`: zero clock snaps and `audio_trimmed_frames` 0, against one snap per run before at −0.79 to −3.27 s and 39k–74k frames of audio destroyed every time.** Editor-verified 2026-08-29 across a VRCDN session and an imax51 session in one run: no audio gap above 85 ms on either | `cargo test -p media-engine --test session` (in CI); live: `bm-probe play rtspt://mr.town:8090/imax51 --duration 60 --csv out.csv` — expect no `SnapCorrection`, and read the `av_offset_us` column's shape rather than its final value |
| Live-vs-on-demand inference | the player decides from the source rather than the author: `finite && rangeable` is on-demand, anything else is a live edge, and the descriptor's liveness is an override (default Auto). Rangeable is a 206 *answer*, with an advertised `Accept-Ranges: bytes` accepted as a second arm for a server that answers 200 to a range request; the total on a 206 comes from `Content-Range` and never `Content-Length` (a range-capping proxy makes that the part). A known total is deliberately not required. Every other transport still settles it itself — RTSP/WHEP/RIST force Live, HLS takes it from the playlist. Inferring Live off the absence of both signals records a diagnostic, since that is the one-directional misread | `cargo test -p media-io --test http_source seekability` (in CI, four server shapes). Verified 2026-08-16 with no liveness set either side: VRCDN `https://stream.vrcdn.live/live/vrcdn.live.ts` inferred Live (the diagnostic fired, joined 767 ms, 401 decoded / 400 presented, 0 pool drops, duration 0); `https://mr.town/vod/scope.mp4` inferred Vod (duration 123 743 ms, read-ahead). **The `-listen 1` feeders on :8093/:8094 cannot serve this lane** — see Rig limitations |
| Startup burst (VOD) | the Bank releases the leading `startup_burst` window (default 2 s) unpaced at every anchor — startup and post-seek — then returns to the 1x + lead schedule; steady-state pacing properties hold with the burst excluded | `cargo test -p media-bank` (in CI) |
| Priming join (live) | live lanes overlap decoder priming with the startup hold: release runs ahead of 1x during the hold (bounded by `startup_burst` beyond the 1x line from the first arrival — a moving cap, so release can never wedge), presentation stays gated until the hold target has arrived (explicit depths hold to the full configured depth, cushion included, so the join delivers the depth the user asked for; Auto lanes hold to the estimator's lag only — join fast, grow on evidence; target-zero lanes lift immediately, preserving the §6.14 sub-second posture), and the 1x schedule anchors presentation-relative when the first frame reaches the viewer, at the whole released span, so release carries straight on at 1x rather than pausing while the surplus above the cushion drains — one anchor governs both tracks, so a pause there starves the audio ring as well as the frame pool; the released-ahead span is in-flight depth downstream, so the bank's own lag reads as what is left un-released; the debt bound and decay run unchanged after the anchor; `startup_burst: 0` restores the strict hold-then-1x startup | `cargo test -p media-bank --test priming` (in CI); integrated: the impairment rows below |
| Per-track release | the release thread never blocks on a full decode channel: the target's messages park in order, the whole track gates in the Bank (`pop_due_gated` — Eos a barrier, cursor advances only in-order so banked/lag grade from the laggard, `pop_due` = the empty gate byte-for-byte), and the other track keeps releasing past it. Audio-leading live joins (RTSP: audio flows from SETUP, video waits ≤ ~10 s for an IDR) bank the audio upstream instead of wedging release, and keep everything against a full ring — a deep explicit hold's primed audio survives, the CI impair row's `audio_ring_drops` being 0. Nothing is shed at a join any more: the pre-join shed belonged to the video-led start and went with it | `cargo test -p media-bank --test gated` (in CI); integrated: the CI impair row's `audio_ring_drops` column + bench (not play — joins must be measured, GOP-phase luck hides wedges) on the mediamtx rtsp/rtspt stereo + slowjoin lanes |
| Audio-leading start | **every live session, with no opt-out** — there is no descriptor field and no component toggle: the session starts audible at the first banked PCM (the audio-only clock-start path), video appears at its keyframe against the running clock; the pre-join shed and the preroll wait are both **deleted**, not merely bypassed — the first banked audio is the join, so neither has a reachable case; the picture can trail the sound by the video decoder's input depth, which is the accepted trade. On-demand lanes are untouched, their legs arriving together. A descriptor still carrying `"audio_leading"` is ignored rather than refused, the struct not denying unknown fields | `bm-probe bench <rtsp-lane> --live` (startup then measures time-to-first-audio; ~2.07 s on the mediamtx slowjoin lane vs ~9 s video-gated, 2026-08-14) |
| Impairment, CI lane | the worst phase-0 jitter profile (ts-rtt300-loss005) through the full engine at 3 s depth over a 1x-paced fixture; pass = session alive, presentation flowing, measured stall within the sizing model's residual. `--depth-ms` sweeps the depth: the gate runs the 3 s cell only, so a depth-sensitive regression can ride a green gate — sweep 1000/2000/3000 by hand when the Bank's schedule or its anchoring changes | `bm-probe impair fixtures/h264-aac-320x180-30s.ts --profile ts-rtt300-loss005 --duration 25 --depth-ms 3000` (in CI, bounded to 25 s) |
| Impairment, full profiles | every phase-0 capture full-length, VOD-file or live-URL lane (`--profile` × `--depth-ms` per the sizing table; the 0.5%-loss profile is the throughput regime — informational, not a gate, §3.4). URL lanes gate on survival + flow (the model's instant-recovery assumption doesn't hold over a real WAN); the deterministic file lane gates on the model | `bm-probe impair <file.ts\|live-url> --profile <name> [--depth-ms N] [--csv out.csv]` (release gate, by hand). VRCDN's 24/7 channel `https://stream.vrcdn.live/live/vrcdn.live.ts` is the authoritative live lane |
| Reconnect/resilience | a dropped live connection (and live EOF — indistinguishable from a dropped connection-close body) rebuilds the transport with jittered backoff, keeps the Bank and generation, rejoins mid-GOP, and narrates itself as Reconnect events; exhausted attempts end the session (Ended for EOF, Error for I/O loss) | `cargo test -p media-engine --test reconnect` (in CI); by hand: play a feeder with `--live` and restart it mid-run |

### Transports and sources

| Row | What it proves | How to run |
| --- | --- | --- |
| Headless playback, file | full pipeline at 1x: decode/present counts, zero pool drops, audio at hardware cadence | `cargo run -p media-engine --example smoke -- fixtures/h264-aac-640x360-30fps.mp4 5` |
| Headless playback, TS file | the same through the ported TS demuxer (container sniffed, PIDs from the PMT) | `cargo run -p bm-probe -- play fixtures/h264-aac-640x360-30fps.ts --duration 5` |
| Auto liveness costs one connection | `Auto` is the default liveness on both sides (`SourceLiveness::default()`, and `BasisMediaPlayer.liveness`, which only puts the field on the wire when it is Live or Vod), so a plain live URL is settled by probing — and the probe is a GET whose 200 *is* the stream. The engine used to drop that response and have the live lane open its own, which spends a TCP and TLS handshake out of the sub-second join budget, walks the redirect chain a second time, rejoins the edge later than it left off, and is refused outright by an origin serving one client at a time. The live lane now adopts the probe's body: the inference is read off the response headers before any byte is consumed, so the handover carries a stream still at offset zero and the head cache fills normally. A source with nothing to adopt — a ranged one, or one a read has moved off the start — is handed back and the lane opens its own, which is the pre-existing path and stays covered. The row drives a real session against a live TS origin that counts its connections, requires playback a whole GOP past the join and then exactly one connection, and asserts the `CapabilityProbe` event fired so an on-demand source cannot pass it by taking one connection for unrelated reasons; it reads 2 with the handover declined. Windows-gated with the rest of that file, since it decodes | `cargo test -p media-engine --test reconnect` (in CI) |
| Headless playback, HTTP | the same through media-io (ranges, pinned connect) | serve `fixtures/` locally, then `cargo run -p bm-probe -- play http://127.0.0.1:<port>/h264-aac-640x360-30fps.mp4 --duration 5 --allow-local` |
| Headless playback, HTTP-TS live | the async I/O domain: sequential streaming source, per-read stall detection, Bank in lag mode (liveness stated via `--live`, never inferred) | `python tools/live-ts-server.py <file.ts> <duration_s> <port>`, then `cargo run -p bm-probe -- play http://127.0.0.1:<port>/live --live --allow-local --duration 12` |
| Live-source unit rows | sequential stream through the router, head-cache re-reads, stall as a typed error, cancellable connect, redirect re-vetting, gate | `cargo test -p media-io --test live_source` (in CI) |
| On-demand source cancellation | The ranged lane carries ordinary VOD playback and held no cancel token at all, while its sibling live source raced every connect, redirect and read against the session's. `connect_timeout` and `request_timeout` bound each request but cannot abort one, and the no-range faststart client is deliberately untimed, so nothing but the server ended that read. `Session::close` joins both the opener thread the open runs on and the demux thread the reads run on, so a server that accepts a connection and then stops answering froze the client's main thread — up to six redirect hops on the open, unbounded on the faststart body. The lane now runs on the async client under the same discipline as the live source, which also collapses the duplicate redirect loop and pinned-client builder and takes reqwest's `blocking` feature off the manifest, so it no longer runs a background runtime beside the engine's own. Two rows against a server that answers and then goes quiet, one per thread: an open cancelled after 200 ms, and a read cancelled after 200 ms once the served head is drained. Both assert the elapsed time as well as the message, since a 20 s request timeout produces a refusal too — they come back in ~200 ms and fail at 20 s with the race removed. The seven rows either side of them are the regression surface that matters: ranged reads, chunk-boundary reopens, the sequential faststart fallback, redirect re-vetting, the redirect cap and the gate all run through the new client unchanged | `cargo test -p media-io --test http_source` (in CI) |
| One connection per sequential open | an origin that answers the opening ranged probe with a 200 has already handed over the whole entity from byte 0, so that response is the sequential stream. The open used to discard it and reconnect for the body, purely so the second request could ride a client with no request timeout — the branch reads the status and nothing else, and every live edge answers 200, so every live open paid two connections. The origins that answer this way are the ones least able to give a second one: a feeder serving one client at a time has nothing left once the probe has taken its slot, and reconnecting lands in its respawn gap. One client now serves the whole source and the timeout moves to the request, which only a body bounded in advance can carry; the opening request takes none, and a link that goes quiet mid-body is held by the client's read timeout, which measures the wait rather than the exchange. The rows are a server that accepts exactly one connection and then stops listening — the open fails outright with the reconnect restored — and a stalled body that must come back as a typed read error inside 2 s against a 500 ms read timeout, which never comes back at all without it. The ranged lane keeps its per-request total bound, so its chunk-boundary reopens are unchanged | `cargo test -p media-io --test http_source` (in CI) |
| Transport errors name their cause | every reqwest failure in media-io was reported as `e.to_string()`, and reqwest's `Display` stops at the outermost layer — "error sending request for url (…)", with the refusal, the TLS alert or the resolver's answer one `source()` hop below it and thrown away. A diagnosis with nothing in it is what an unreachable origin produced. All six sites (the pinned client build, the send and the body reads, on both the ranged and the live source) flatten the chain into the detail, and so do WHEP's five, which run their own requests over the same discipline — the client build, the POST, the PATCH, the DELETE and the SDP body read — through the same `media_io::error_chain`. A row on each side opens against a port nothing is listening on and requires the word the OS supplies rather than the one reqwest does; both fail with the walk removed | `cargo test -p media-io --test http_source` + `cargo test -p media-whep --test signal` (in CI) |
| Zero-length reads | the buffer's length belongs to whoever calls `read_at`, so zero is a length a demuxer may pass and asking for nothing should cost nothing. The on-demand source read a zero from its own copy-out as "the chunk is spent", which an empty buffer produces without the chunk being spent: the loop replaced a chunk it was still holding, losing the unread remainder, and then kept pulling — every following copy-out reporting zero as well — until the body ended or the session was cancelled. The live source already refused an empty buffer at the same boundary, and the on-demand lane does now. The row is pinned against a server that serves a head and then goes quiet, because a read that reached the network cannot come back inside the window while one served from the chunk in hand can; without the guard it fails at twenty seconds, on the request timeout | `cargo test -p media-io --test http_source` (in CI) |
| Resolve ceiling and open cancellation | Nothing bounded the platform resolver. `connect_timeout` and `request_timeout` are client settings that only start once an address set exists, so a hostname delegated to a nameserver that never answers cost whatever the OS decided, multiplied by the redirect cap since every hop re-vets a host the server chose. Every resolve in media-io now runs under one ceiling, and the async lanes resolve off the reactor: a blocking syscall inside a poll cannot be pre-empted, so the live source's `select!` against the session token, and WHEP's signalling futures sharing a two-worker runtime with every reader task in the process, were racing something they could never observe. The rows pin the property rather than the latency, which needs a blackholed nameserver to see: a resolve is polled to Pending before it answers, so a `biased` sibling branch still runs, and the row fails with the blocking call restored. WHEP's signalling is then raced against the engine's teardown probe, since `bm_session_close` joins the thread it runs on straight from the client's main thread — the row holds a POST open unanswered, flips the probe after 200 ms and requires the open back inside 2 s against a 20 s request timeout, where it lands in ~250 ms. A failing live open also retires its own child cancel token: no source is constructed, so no drop does it, and a playlist lane re-opening per segment would leave a parked forwarder task on the shared runtime per refresh. The mechanism that makes the WHEP cancellation possible — blocking the calling thread on the runtime — is also a constraint on it, since that panics inside tokio on a runtime worker, and `WhepDemuxer::open` is public. It screens for a runtime context ahead of even parsing the URL and refuses by name; the row drives an open from inside a runtime and fails on tokio's own assertion with the screen removed. An open that fails after the POST also has a teardown to discharge: the server has created a session, and every failure from that point returns before the demuxer exists, so the `Drop` that owns the DELETE never runs and the server holds the session until its own idle timeout. The resource is named by then, unlike a POST abandoned in flight, so the DELETE goes out on the way past — at every fallible step after it is known, not only at the PATCH. The ceiling bounds the wait rather than the syscall, so a timed-out lookup holds its permit for as long as the platform resolver takes: a peer naming enough blackholed hosts makes every later resolve in the process refuse at the ceiling, and the refusal used to name the nameserver, which is the wrong place to look. A row drives the queued half against a full semaphore and pins that the message names the saturation instead. The refusal is only half of it, though: a lookup that queues and then succeeds surfaces nothing, so the pressure before the first refusal was invisible. Queued lookups are counted and the first and every sixty-fourth reported, which separates a pool under load from one host answering slowly without costing a line per resolve. The blocking form parks its thread on the shared runtime, which panics on a runtime worker, and `resolve_vetted` is public — so that is a property of the callers rather than of the function. Both of them today, the RIST and RTSP opens, are plain synchronous functions on the opener thread; a debug assertion puts a future async caller in front of a failing test rather than tokio's own assertion in the field. | `cargo test -p media-io --lib` + `cargo test -p media-whep --test signal` (in CI) |
| Resource fetch caps | the whole-resource fetcher bounds what a playlist lane will buffer, on both of its arms. The HTTP arm caps inside the read loop; the filesystem arm opens once, takes the length from the handle rather than from a second resolution of the path, and reads through a `cap + 1` bound, so the bytes returned are capped whatever the filesystem stated for the name — a resource that grows, or is swapped for a symlink to something larger, between the size check and the read cannot turn a bounded fetch into an unbounded allocation on the demux thread. A resource at exactly the cap still serves whole (the bound is inclusive); one byte past refuses naming the cap it enforced; a missing file stays an I/O error rather than a cap refusal, since the two are different diagnoses. The race itself is not reproducible in-process, so these rows pin the invariant that makes losing it harmless rather than the window | `cargo test -p media-io --test resource_fetcher` (in CI) |
| Playlist origin confinement | what a playlist may reach is fixed by where the playlist itself came from, and never widens. A playlist fetched over the network gets a fetcher with no filesystem arm at all, so none of the spellings that reach the disk on Windows can be read however the URI is written — the drive-relative `c://Windows/win.ini` that survives URI resolution intact because it carries `://`, the ordinary `C:\…` path that URL joining rewrites into the same drive path, `file:` URLs, and the UNC form that would otherwise be an outbound SMB connect the address gate never sees. A playlist opened from disk keeps both arms, since reaching the network from there is what the address gate already covers, so the fixture lanes are untouched. Pinned at both levels because a unit test cannot see the wiring: the fetcher's own refusal, and an end-to-end session where an HTTP-served playlist names a real, readable, playable copy of the TS fixture planted outside its origin and must settle in Error having decoded nothing, with the address gate deliberately wide open so a pass cannot be the gate refusing loopback by accident | `cargo test -p media-io --test resource_fetcher` + `cargo test -p media-engine --test hls_origin` (in CI) |
| Source routing | what a session's URL names is decided once, from the URL parser's normalised scheme, and a local path is a case of that decision rather than its fallthrough. Schemes are case-insensitive (RFC 3986 §3.1) and the managed classifier compares them that way, so `HTTPS://…` reaches the HTTP lane rather than being opened as a file; the scheme is lowercased for the transports below, which match it as text, while every other byte including case is left as the caller wrote it. A scheme the engine does not carry (`ftp:`, `file:`, `gopher:`, `data:`, `javascript:`, `smb:`) is a typed config refusal — before this every one of them was opened as a path. A UNC path is refused unless the session has opted out of the address gate, since Windows resolves both `\\host\share` and `//host/share` through the SMB redirector and opening one is an outbound network connection, with the process's credentials, that the gate never sees. All four leading-separator pairings are rows, since Windows takes either character in either position and every one of them opens the same share. Two leading separators are not always a host, though: `\\?\` and `\\.\` open the device namespace, where `\\?\C:\clips\x.mp4` is the long spelling of a local file — the spelling a caller reaches for past MAX_PATH — and only the UNC device inside that namespace names a host. Rows run in that direction too, and the prefixes are matched with backslashes only, since an extended-length path reaches the object manager unnormalised and the forward-slash pairings are not it. Drive paths, absolute and relative POSIX paths and bare filenames all still classify as files, an ordinary local file still plays end to end, and a caller-supplied source plays under a label that names no transport at all — `open_with_source` states its URL is display-only, so a harness labelling its source must not be refused over the label. One row guards the normalisation itself: the URL parser skips leading control characters and spaces before reading a scheme, so its answer does not always begin at byte zero, and splicing on length alone turns `" http://h/x"` into `"httpp://h/x"`; where the leading bytes are not that scheme in some other case, the input passes through untouched | `cargo test -p media-engine --lib classify_tests` + `cargo test -p media-engine --test routing` (in CI) |
| Playlist URI scheme | a playlist cannot change what kind of thing its resources are. Resolution goes through URL joining rather than a `://` substring test, and the joined result must carry a scheme the fetcher can actually go and get (`http`/`https`), so `file:`, `ftp:`, `data:` and the Windows drive-letter forms are refused where they used to pass through untouched and land on the filesystem arm. Both drive spellings are rows: `c://x` carries the `://` that used to mark a URI as absolute, and `c:/x` reads as a one-character scheme. What real playlists do is pinned alongside and unaffected — relative, root-relative, absolute onto a different host (a CDN serving variants off another origin, which is why confinement is by scheme and never by host), and http↔https. A playlist on disk may still name http(s) resources, since the address gate vets those at the fetcher; only the schemes that would reach its filesystem arm are refused. The scheduler's own rows moved onto a real `https://` base at the same time, having asserted against a synthetic `hls://` one that the engine never opens. The pinned fuzz-campaign playlist now proves only that a hostile input refuses without panicking, which is what a campaign artefact carrying several hostile shapes at once can honestly prove; the duration-cap property it used to stand for has its own row on a playlist that is otherwise well-formed | `cargo test -p media-hls --test hls` (in CI) |
| Playlist directory confinement | a playlist opened from disk resolves its URIs beside itself and nowhere else, screened at both ends. The end-to-end row for the other direction — a disk playlist reaching its own segments — runs on every host now: it asserts the error category rather than a decoded frame, so a host with no decoder still exercises the fetcher wiring that the picture-bearing row could only cover on Windows. Resolution refuses any URI that is not a plain relative name — absolute, `..`-walking, Windows drive-relative (`c:seg.ts`, absolute to the OS while `is_absolute` says otherwise) and UNC — before a path is built at all, while `./seg.ts` and `nested/seg.ts` still resolve. Which screen catches the drive shapes is worth knowing if that code is reworked: a drive-shaped string parses as a URL whose scheme is the drive letter, so the unfetchable-scheme arm refuses it first and the drive-prefix check behind it is never reached — measured, and kept as depth rather than as the thing doing the work. The fetcher then re-screens what it is handed against the playlist's own directory: a lexical pass over what the playlist contributed to the path (the root is the caller's and may legitimately carry `..`, so only the contributed part is judged), then a canonicalising pass that resolves links, which is what catches a link planted inside the directory whose every component looks ordinary. Exercised with a real directory link — Windows symlinks need Developer Mode, so the row falls back to a junction, which `canonicalize` resolves identically and an ordinary account can create; it announces a skip only if the host allows neither. The origin rows' planted fixture is removed by a drop guard rather than at the end of the row, so a failed assertion cannot panic past the cleanup and leave its process-named directory behind — which is how a hundred of them accumulated before. **Residual, deliberate:** what is opened is the canonicalised path, so a component swap between that check and the open is not closed. Doing so needs openat-style handle-relative resolution, which is not on stable Rust for Windows (`windows_by_handle`, rust-lang/rust#63010) and not reachable from a crate that forbids unsafe. The canonicalising pass still pays for itself: without it the same attacker wins with a planted link and no race at all. All three refusals share the word "directory", so the rows now name the message each URL must draw — the absolute path fails to strip the root, the traversal walks out of it after stripping — and a change reclassifying one as the other goes red where it used to pass | `cargo test -p media-hls --test hls` + `cargo test -p media-io --test resource_fetcher` (in CI) |
| HLS VOD playback | playlist-sniffed routing (file or HTTP), TS chaining through one demuxer / per-segment fMP4 with absolute tfdt timestamps, full pipeline to Ended with zero pool drops | `cargo run -p bm-probe -- play fixtures/hls/ts/index.m3u8 --duration 8` and `…/hls/fmp4/index.m3u8`; seek via `bench` on the same lanes |
| HLS live | live playlist (no ENDLIST) ⇒ Bank lag mode from the playlist's own statement; join three segments back; window slides in real time; loop-point EXT-X-DISCONTINUITY splices play through; ENDLIST ends the session | `python tools/live-hls-server.py fixtures/hls/ts <duration_s> <port>`, then `bm-probe play http://127.0.0.1:<port>/live.m3u8 --allow-local --duration 25` |
| HLS over real HTTPS | the VOD lanes against a real origin (206 ranges on segments, 5.1 audio) and a public multi-variant master playlist | mr.town `https://mr.town/vod/hls_imax/index.m3u8` + `…/hls_fmp4/index.m3u8` (bring `tls443` up first, down after); `https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8` for the master-playlist path |
| RTSP lanes | `rtsp://` negotiates UDP first (media-rtp reorder/jitter/RTCP-RR layer under retina's signalling) and falls back to TCP-interleaved when SETUP fails or no datagram arrives within 5 s; `rtspt://` pins TCP (verify no UDP SETUP is attempted). Rows on both transports: stereo, 5.1 (mediamtx fragments its large AAC AUs — reassembly exercised, over reordered UDP delivery too), the adversarial ~10 s-GOP mid-GOP join, publisher-restart reconnect (RTCP BYE ends the session as source loss; failed re-opens consume the attempt budget; rejoin renegotiates UDP), and the forced-blackhole fallback (block the server's outbound RTP/RTCP, e.g. `iptables -I OUTPUT -p udp --sport 8000:8001 -j DROP` on the box, and confirm the TransportFallback event + TCP playback); RTCP-SR-based A/V alignment with a bounded join-skew fallback | mr.town `rtsp://mr.town:8090/{imaxstereo,imax51,imaxsilent,imaxslowjoin}` + the same paths via `rtspt://` (bring `mediamtx` + `mtx-pub-*` up first, down after; restart a publisher mid-run for the reconnect row); authoritative: `rtsp://stream.vrcdn.live/live/vrcdn` |
| RTP timestamp scaling | converting RTP clock units to microseconds is where a sender's own numbers become the engine's timeline, and the span being converted has no ceiling by design: extension has to unwrap across cycles for a long-running stream, so the receiver bounds packet geometry but never timestamp magnitude. Each accepted packet can move the unwrapped total by up to 2^31, which takes a plain i64 scale past what it survives inside a few thousand packets — under a second of traffic at any real rate — and the wrap is silent in a release build. What it lands on is either the presentation timestamp stamped on every emitted access unit or a stream's NTP-at-zero alignment anchor, both of which then flip sign and magnitude arbitrarily. One helper widens before the multiply and clamps on the way back, at all nine sites across the RTSP TCP and UDP lanes and WHEP — the per-frame conversions, the two aligners and the RTCP sender-report handler. The five places that rebase a timestamp against the stream's first one saturate their subtraction too: the pre-join guard bounds that difference below but not above, so an ordered pair of extended timestamps far enough apart overflows before the conversion ever sees it. The alignment anchor a sender report produces goes through one helper as well, which the three transports had each hand-rolled: the two UDP lanes branched on the sign of the offset and the TCP lane did not, so a negative elapsed there saturated the offset to zero instead of moving the anchor the other way — reachable in principle, since retina's `Timestamp::new` guards only the underflow of `timestamp - start` and leaves `elapsed()` free to be negative. The final pts add saturates too, being the one place a clamped conversion could still be pushed over by a non-zero alignment offset. Rows pin the saturation at both signs, the exact answer for the first span an i64 multiply cannot hold, ordinary spans unchanged, and the anchor's behaviour at zero, at both signs and at both saturated ends. Its integer form is also exact where scaling through a f64 is not, though only past ~11 days of elapsed and only by sub-nanosecond amounts — that part is a contract the row pins, not a defect an anchor would have shown | `cargo test -p media-rtp --test receiver` (in CI); the RTSP/WHEP lane rows above are the end-to-end guard |
| Depacketizer drain discipline | retina's depacketizers carry a contract the types do not state — `push` must not be called until `pull` is exhausted — and every codec enforces it with a `panic!` rather than an error: H.264, H.265 and JPEG on a queued item, AAC and ONVIF on their state machines. A refused push does not mean nothing was queued. H.264 enqueues either a completed access unit or the error describing one and *then* rejects the same packet, which a timestamp change mid-fragment does in a single call. So the two lanes that treat a refusal as per-packet loss rather than session failure — WHEP and RTSP-UDP; the TCP lane ends the session on one and never pushes again — were skipping the drain in exactly the case that needed it, and the following packet panicked the session task. Three packets from the sender are enough: an FU-A start, a packet whose timestamp moved mid-fragment and whose own first byte has the forbidden-zero bit set, then anything at all. RTSP-UDP is the wider surface of the two, carrying no SRTP, so the sequence need not come from the server the URL names. Draining unconditionally honours the contract for every codec rather than for the one that was found, and costs nothing on the ordinary path since the queue is empty there. Rows stage that exact sequence through the real receive path — parsed fields via `on_packet` for WHEP, a hand-built RTP datagram via `on_rtp` for UDP — drive the lane's own drain function, and assert it returns rather than panicking; both fail with retina's own `push with data already pending` if the drain is made conditional again. The refusal itself is no longer discarded: a lane nothing can depacketize read as a healthy one carrying no packets, so refusals are counted and the first and then every 256th is reported with the running total. The WHEP lane counts and reports the same way — the first sweep fixed the RTSP site and left the identical shape one crate over. Both rows assert a refusal actually happened, which is what makes them cover the push that panics rather than infer it from the drain returning. The UDP row also drains a millisecond past the configured reorder wait rather than at the instant its datagrams were fed, so their release is the receiver's contract rather than its current policy | `cargo test -p media-whep --lib` + `cargo test -p media-rtsp --lib` (in CI) |
| AAC access units fragmented by marker bit | RFC 3640's fragment form states the whole access unit's size in every fragment's AU-header, and that is the rung retina keys on: a header claiming more than its packet carries starts a fragment. mediamtx's RTSP output instead sizes each fragment's header to that packet alone and sets the marker bit only on the last, so the header always matches what arrived, the size rung can never fire, and every fragment reads as a complete access unit. Delivered separately, one access unit reaches the decoder in pieces and decodes to loud broadband noise: measured output reached 552x full scale against a source mastered to exactly 0.0 dBFS, in bursts of a few tenths of a second scattered through a run, with roughly one join in four audibly damaged. Only multichannel content shows it, because a stereo access unit fits one packet and always carries the mark - which is why the stereo lane on the same server is clean. Both transports fragment, so this is not a UDP-loss effect and TCP-interleaved does not escape it: measured audio access units reach 1800 bytes on UDP and 1846 on TCP against a 1436-byte payload ceiling. Reassembly accumulates to the marker bit, and does so only once the stream has been seen to set it: a sender that never marks anything says nothing by leaving it clear, and still gets an access unit per packet as before. With no total stated up front the accumulation carries its own bound, the 13-bit AU-size field's ceiling, so a sender that withholds the mark indefinitely is refused rather than growing the buffer without limit. Loss mid-reassembly discards the prefix rather than splicing across the gap. Four of the six rows fail without the fix; the two that pass pin the behaviour that must not change, which is what makes them worth keeping | `cargo test -p media-rtsp --test aac_reassembly` (in CI); by hand against the rig, `cargo run -p bm-probe --release -- play rtsp://mr.town:8090/imax51 --duration 20 --audio-out out.f32` and check the sample peak sits near 1.0 rather than in the tens or hundreds |
| WHEP lanes | `whep://` / `wheps://` (§6.13, the sub-second lane): hand-rolled signalling over gate-vetted pinned HTTP — both the 201+answer and 406+counter-offer/PATCH flows, ICE gathered fully before the POST (host candidate rides in the offer, so PATCH-refusing servers work; the direct flow never PATCHes), `Link rel="ice-server"` parsed and surfaced (not used for gathering — a check-initiating receive-only client needs no srflx and TURN is out of scope), DELETE fired on teardown; str0m (sans-IO, RTP mode, wincrypto on Windows) runs ICE/DTLS/SRTP/RTCP on our socket; decrypted RTP goes through media-rtp's reorder, retina's H.264 depacketizer / RFC 7587 Opus, and the RTSP lanes' shared aligner/emit path (SR-based A/V alignment included); every media-path address str0m wants to reach is gate-checked at the transmit boundary (§9.3 — a blocked candidate's connectivity check is never sent); Bank rides at the decoder-cushion floor (§6.14 shallow posture, explicit depth still wins); publisher restart ⇒ feed stall ⇒ full re-signalling via the engine reconnect path. Codecs: H.264 + Opus only, offered as such (WebRTC carries neither B-frames nor AAC — a B-framed publisher's video track is closed by mediamtx server-side) | `cargo test -p media-whep` (in CI: signalling flows, PATCH discipline, redirect re-vet, Link parse, blocked-candidate row, feed stall); by hand: local mediamtx + `ffmpeg … -bf 0 … -c:a libopus -f rtsp rtsp://127.0.0.1:8554/test`, then `bm-probe play whep://127.0.0.1:8889/test/whep --allow-local`; mr.town: bring `mediamtx` + the transient `whepav` publisher up per the endpoints doc, `bm-probe play whep://mr.town:8091/whepav/whep`, down after |
| WHEP signalling body cap | the signalling answer is the one HTTP body the engine buffers whole, out of a host a peer chose, so it is bounded by bytes and not only by the request timeout — a server that writes at line rate for the 20 s it is allowed decides how much the client allocates otherwise, on the opener thread, where nothing rate-limits it. `IoLimits::max_signalling_bytes` (256 KiB against SDP answers measured in kilobytes) is checked against the chunks as they arrive, because a chunked answer states no length for a pre-check to read. Rows sit either side of the bound and on the shape that motivates it: a body of exactly the cap is still read whole and fails at the SDP parser, so the bound is inclusive and a cap refusal is distinguishable from a malformed one; one byte past refuses naming the cap; the 406 counter-offer arm refuses the same way, since it is the same reader and not a second uncapped one; and an endless chunked body refuses at the cap rather than running to the timeout, which the row pins by asserting the elapsed time as well as the message — the bound is 2 s against a 20 s timeout, loose enough not to flake on a host running the rest of the suite, and the refusal in practice lands around 30 ms. Driven through `WhepDemuxer::open` rather than the signalling function, so the row covers the wiring as well as the reader. That cap is also what bounds the session's list of refused candidate destinations: remote candidates arrive in the answer and nothing in the session loop takes a trickled one afterwards, so the list was bounded in the low thousands by the answer's size rather than by anything of its own. It states a bound now — sixty-four held and named, with the line where it stops saying so — and the decision sits in a function a row can drive, since driving it through str0m would need a peer offering a candidate list and this crate has no way to stand one up | `cargo test -p media-whep --test signal` (in CI) |
| RIST lanes | `rist://` (Main profile, caller) through librist behind the `rist` cargo feature: librist owns sockets/ARQ/jitter/PSK-AES and serves recovered TS into the ordinary live-TS pipeline; the host is resolved + gate-vetted and librist pinned to the vetted literal; plain and AES-128 (`?secret=…&aes-type=128`) both play 5.1 at 16 Mbps with zero pool drops; a wrong secret fails typed at the container sniff (librist delivers undecryptable bytes, nothing plays); a `rist`-less build refuses `rist://` with a typed "not built" error; a panic caught in the data callback is counted and reported on the first and every 256th rather than only absorbed, since this lane drops datagrams by design and an unreported panic read as ordinary loss on a capture. That callback checks its own argument as well as the block it is handed — both come from librist, and a fence catches an unwind rather than a bad dereference — and the ring's reader recovers from a poisoned lock as its writer and drain already did, since every one of them leaves the ring consistent and one unrelated panic otherwise turned every later read into a second panic on the engine's reader thread. Requires the staged librist static (`tools/build-librist.ps1`, pinned v0.2.11) — CI's rist rows skip loudly when it is absent | local loopback: `ffmpeg -re -stream_loop -1 -i fixtures/h264-aac-640x360-30fps.ts -c copy -f mpegts "rist://@127.0.0.1:11968"`, then `cargo run -p bm-probe --features rist -- play rist://127.0.0.1:11968 --duration 12 --allow-local` (AES: add `?secret=…&aes-type=128` on both sides); mr.town: bring `rist5000`/`rist5001` up (not in the preflight — verify with `ss -unlp` on the box), `bm-probe play rist://88.208.227.151:5000 --duration 20` + `…:5001?secret=<key>&aes-type=128`, down after |
| RIST on Android | the transport ships on Quest, not just Windows and Linux. `media-rist/build.rs` has an `android-arm64` arm, `tools/build-librist-android.sh` cross-builds the static against the NDK through a meson cross file, and **every** Android build path now passes `--features rist` (the staging script, the release job, and the CI clippy row when the static is staged) — the feature being off in every build path while the code was present is exactly why this was outstanding. Verified 2026-08-16 on this box: clippy green for `aarch64-linux-android --features rist`; the release `.so` links and stages at 9.6 MB (inside the <=12 MB budget) with `rist_receiver_create`/`rist_start`/`rist_destroy` and 346 vendored mbedTLS symbols in the unstripped artefact, and NEEDED unchanged (liblog/libmediandk/libdl/libm/libc — the static pulls in no new shared dependency) | `bash tools/build-librist-android.sh` then `.\tools\stage-android-plugin.ps1` |
| Split sources | `audio_url` alongside `url`: a video-only source played against a separate audio-only one, which is how adaptive ladders serve every rung above their muxed fallback. Two demux threads feed the one Bank, so the buffering model, the clock and the release schedule stay the session's. The audio leg's track ids are namespaced (both demuxers number from zero and the Bank routes on the id alone), each leg contributes only its own kind of track, the video leg owns seek and the audio leg follows it to the same landing on the same generation, and the session's Eos is banked only once both legs are exhausted — re-offered on every idle tick rather than decided on one edge, so the handshake cannot be lost. Legs are held within `SPLIT_LEAD_CAP_US` (100 ms) of each other, measured as how far each has come from its own timeline origin and never as a distance between absolute dts: container timelines start where they like — an arbitrary 33-bit clock on MPEG-TS, a `baseMediaDecodeTime` on fMP4, a first cluster timestamp on Matroska — and against a fixed zero baseline every one of those read as both legs already sitting a whole timeline ahead of each other, so both waited, neither banked, neither baseline moved and the session stayed in Buffering with no audio, no video and no typed failure until it was closed. A seek moves both baselines, each leg clearing its own as its own demuxer lands so no pre-seek AU can latch one at the position it is leaving — and its banked dts goes with that baseline, since the legs observe the seek at their own pace and the audio leg can bank one more access unit from the timeline it is leaving after the video leg has landed and rebased. Pairing that with the baseline it latches at the landing makes the video leg's view of its progress a difference across two origins, negative when seeking forwards, which holds the video leg out of the Bank until the audio leg banks again — the landed position is absolute, and two separately muxed legs need not agree on where it falls on their own timelines, so it is not a baseline either of them can be handed. A leg that has banked nothing has come no distance rather than sitting at a placeholder, which keeps the ratchet the cap is there for: the Bank releases in arrival order on a dts schedule, so a leg that reads ahead puts not-yet-due events at the head and, if it outruns the Bank's read-ahead depth, fills it and locks the other leg out entirely — a deadlock, since the clock waits on the video leg. On-demand HTTP(S) and files only; a split request on a live transport, an HLS playlist or a caller-supplied source refuses typed. Two sources that disagree about where their timelines start refuse typed as well, with their own sub-code in the descriptor's error category: the Bank's depth is one span across everything banked, so a gap between the origins is counted as media it holds, and past the decoder cushion that alone makes it read as full from the trailing leg's first access unit — see Known limitations for what that wedges | `cargo test -p media-engine --test split_source` (in CI: both legs to a natural end, seek taking both legs, per-leg track filtering with the same muxed file on both legs, a pair whose timeline starts at ~1.47 s rather than zero, the live-transport refusal, a TS video leg against an m4a audio leg ~1.4 s apart refusing rather than wedging) plus `--lib pipeline` for the cap's own contract across a seek and for the origin gap being a distance that cannot wrap, both asserted directly because the pair that would exercise them end-to-end is the pair the engine now refuses (see Known limitations). Integrated, and a CI row: `bm-probe play fixtures/split/h264-640x360-30fps-video.mp4 --audio-url fixtures/split/aac-48k-stereo-audio.m4a --duration 9` — expect 180/180 presented, 0 pool drops, ~287.7k audio frames, natural Ended. **Soak it**: this lane's failure mode is a timing-dependent wedge that instrumentation hides, so judge changes here by ~20 repeat runs, not one. A short audio total is **not** that wedge and not a product fault: `run` pulls audio at wall-clock cadence, so what it counts measures how the test thread was scheduled as much as what was delivered, and the whole workspace suite running alongside it can cost a second the run never makes back. `AUDIO_LEG_PLAYING` sits well under the fixtures' six seconds for that reason — it separates a leg that is playing from one that was dropped, and a dropped leg pulls approximately zero. The wedge shows as the session never reaching Ended, or the video count falling short, not as an audio total a little under the bar |
| Audio-only file lanes | each raw audio container plays through the full engine to a natural End at its stored duration: FLAC, Opus and PCM in-process, MP3 and AAC(ADTS) through MF; audio thread owns the clock and position | `cargo run -p bm-probe -- play fixtures/sine-48k-stereo.{flac,mp3,aac,opus} --duration 9`; `... play fixtures/sine-48k-stereo.wav --duration 4` (96 000 frames @ 48 kHz x 2) and `... play fixtures/sine-48k-51.wav --duration 4` (48 000 @ 48 kHz x 6) |
| Codec batch (mr.town /vod/) | the full breadth over real HTTPS: FLAC 16/24-bit, 5.1, embedded-art and variable-blocksize; MP3 CBR/VBR/ID3; Ogg Opus; VP9 WebM ×4 layouts + VP9-in-MP4; AV1 WebM, fragmented MP4 and 1080p60 AV1+AAC (rav1d holds 60 fps) — all play at 1x with zero pool drops | bring `tls443` up, `bm-probe play https://mr.town/vod/<lane>` per the endpoints doc, down after |

### Shared playback and sync

| Row | What it proves | How to run |
| --- | --- | --- |
| Sync soft target (§8.4) | shared-playback receivers feed the owner's extrapolated position via `bm_session_set_sync_target` (negative clears) and the engine runs the ladder — dead band 150 ms (no action), then a ±2% slew, then a seek only past 2 s (the last rung, never the first); the wanted slew surfaces as the snapshot's `sync_rate_ppm`, which the managed audio pull applies through its resampler on audio-master lanes (a consumer that ignores it degrades to the seek rung); wall-master (video-only) lanes are slewed engine-side (`MediaClock::slew_wall`); live sessions ignore targets (§8.5); the target extrapolates at 1x between calls, so one call per received heartbeat converges | `cargo test -p media-clock` (slew_wall rows) + `cargo test -p media-engine --test session sync_target` + the `sync.rs` ladder units (in CI); `SyncSlew`/`SyncSeek` diag events carry the error and applied rate |
| Divergence bound (§8.5) | live lanes state `max_divergence_ms` in the descriptor (managed `maxDivergenceMs`); it lands as a ceiling on the Bank's lag cap, which also clamps Auto's depth growth — an explicit depth beyond it fails typed through the Bank's own validation; live position is never chased peer-to-peer | covered by the Bank's config validation rows; by hand: open a live lane with `"max_divergence_ms"` set and confirm Auto depth stays inside it in the capture CSV |

### Platforms

| Row | What it proves | How to run |
| --- | --- | --- |
| Linux headless lane | the engine builds and runs headless on `linux-x64` (the CI half of the Linux backend): software floors route (AV1 on rav1d without its non-PIC asm, FLAC, Opus), platform codecs refuse typed (H.264/H.265/AAC/MP3 — no VAAPI adapter yet), the sink consumes due frames (position/EOS/counters flow with no present target), and the capability blob claims only what the build decodes; the full test suite, conformance gate and the software-decode play row all pass on the GPU-less VPS | on a Linux host: `cargo fmt --check && cargo clippy --workspace --all-targets --examples -- -D warnings && cargo test --workspace`, then `bm-probe caps --compact` (expect the linux-x64 shape), `bm-probe play fixtures/mkv/av1-opus.webm --duration 8`, `bm-probe conformance fixtures` (needs ffprobe); `tools/ci.sh` runs the lot and skips the impair row loudly where H.264 decode is absent (debug-build rav1d without asm decodes below real time — the play row grades pipeline health, not rate) |

The Quest matrix has its own section below — those rows are device rows and
none of them run in CI.

### Harness and instrumentation

| Row | What it proves | How to run |
| --- | --- | --- |
| Bench lane | the §11 budgets measured mechanically: startup-to-first-frame and seek-to-settled per lane, aggregated over runs (with the startup burst: ~133 ms TTFF / ~56 ms seek on the A/V MP4 fixture, vs ~880/~890 ms without) | `bm-probe bench <src\|url> [--runs N] [--seek-to-ms N] [--live]` |
| Capture recorder | the diagnostics timeline as CSV (stable column contract) | `bm-probe play <src> --csv out.csv`; feed to the basis-buffer-analysis tooling |
| Engine A/V offset is measurable | the presented video pts minus the audio playhead reaches the ABI snapshot as `av_offset_us`, the Unity frame capture as its trailing column, and `bm-probe play`'s `a/v:` line. It also reaches the engine’s own capture recorder, as the trailing `av_offset_us` column after `audio_trimmed_frames` — added 2026-08-28. Until then the only headless view of it was `bm-probe`’s single end-of-run summary line, so nothing outside the Editor could see the offset’s **shape over time**, and that gap is how a presentation lag came to be read as a clock error: the offset walks over tens of seconds while the clock sits in the dead band emitting no corrections at all, and only the trajectory distinguishes the two. A headless run on the 5.1 RTSP lane now shows the walk directly — −678 ms at 7.5 s to −43 ms at 43.6 s — which is the same shape an Editor capture shows, so the question no longer costs editor time. `i32::MIN` means one side is not known yet, **and that is scoped per generation**: the gate asks whether *this* timeline has presented, not whether anything ever has. `Present.out_count` is cumulative and survives a flush, so on its own it stays true across a seek or a reconnect and the window before the new timeline presents exports the old video position against the new audio playhead — a plausible-looking number where the honest answer is absence, and it now reaches a capture column where that is worse than a gap. The per-generation signal is the presentation origin, and **presentation arms it, not the clock**: an audio-leading start has the ring start the clock, so the video thread never reaches the clock-start branch and gating on that alone reported the sentinel for the whole session — on every live lane, since every live session is audio-leading. `note_presented` arms the origin at the first frame presented in the generation, first writer winning so a video-led start keeps the clock's own origin, and a flush clears it so the next generation arms its own. Measured on the impairment lane: **0 of 197 capture rows carried a real offset before, 154 of 197 after**, the remaining 43 being the pre-presentation window where the sentinel is the right answer. `av_offset_us` is factored out so both the gate and the sentinel-collision clamp are assertable. And that includes having no session: the managed per-session readings (`_avOffsetUs` with `_engineChannels`, `_engineSampleRate` and `_syncRatePpm`) are cleared in `Close()`, so a closed player, a refused open and an open that fails all read as the sentinel rather than as the previous session's values. Clearing them there covers the open path as well, because `Open` closes first — the one path that deliberately keeps them is a moderation-blocked open, which returns before `Close` and leaves a still-playing session alone. Without that the diagnostics recorder writes a live-looking offset for an idle player. Nothing steers on it. It exists because the frame capture could not previously state the engine's A/V alignment at all: it had the presented pts (`position_us`) and no absolute audio position, so the only available figure was `position - consumed/48000`, which is the join origin plus the serve trim and not an offset. Note also that clock-versus-playhead is **not** this quantity and cannot substitute for it — video is presented against a clock slaved to the playhead, so that error converges to the dead band by construction whatever the picture is doing. Both terms are read on one audio-thread tick, so the figure is a difference rather than two samples a poller took a frame apart. It took the snapshot's reserved slot at offset 76, so the struct is unchanged at 88 bytes, the ABI stays 4 and every other field keeps its place — the cost of that choice is that a **stale plugin writes 0 there**, which reads as perfect sync rather than as unknown, so confirm the plugin is current before believing a 0 | `cargo test -p media-engine --lib presentation_arms` (in CI) pins what arms the origin. End to end, since no fixture-driven engine row builds a session: `bm-probe impair fixtures/h264-aac-320x180-30s.ts --profile ts-rtt300-loss005 --duration 20 --depth-ms 3000 --csv x.csv`, then count rows whose `av_offset_us` is not `i32::MIN` — a live session that reports the sentinel throughout is the regression. `bm-probe play <live-src>` prints `a/v:`; in the Editor, the frame capture's column. Layout: `cargo test -p media-ffi` (the 88-byte assert) |
| Serve-trim total is a capture column | the audio ring's cumulative serve trim reaches the capture as the trailing `audio_trimmed_frames` column, and `bm-probe play`'s audio summary line prints it. It was the one loss channel on the audio path no column carried: `AudioTrim` narrates it as a rate-limited event, but a capture read after the fact only had it implicitly, as `(audio_ring_in_count - audio_ring_out_count) - (ring capacity - audio_ring_occupancy)`. Reconstructing it that way is easy to get wrong and easy to skip, and a trim reads as A/V drift in any metric built on consumed frames, because a trim advances the playhead and not `consumed`, so a join-time trim reads as a steady offset that never converges. It is session-level rather than a field on `Stage::AudioRing`, whose `drops` already counts whole chunks discarded for an inert consumer: frames and chunks cannot share a column. Appended, so every existing column keeps its name and position. **The total is the session's, not the generation's**, and that distinction is load-bearing: a seek reinstalls the audio generation and resets the shared block, so a figure taken from the per-generation counter falls after a seek and reports less loss than the session had — and the `AudioTrim` event beside it is rate-limited against a high-water mark held across generations, so a fallen counter also silences the event until the new generation passes the old total. The per-generation counter stays, because the ring's own accounting invariant — served plus trimmed equals pushed — only holds inside one generation | `cargo test -p media-diag` (in CI) — the value row is load-bearing: writing a constant in place of the total fails it and only it, seven of eight still passing. End to end: `bm-probe play <live-src> --csv out.csv` and confirm the summary's trimmed figure equals the last row's final column |
| Capture reaches the file | an `Ok` from the capture writer means the rows reached the target, not that they reached a buffer. `CaptureRecorder::write_csv_rows`, which `write_csv` delegates to and which every caller therefore reaches by one name or the other, takes its sink by value and drops it inside the call, and all three callers — the engine's diag sampler, `bm-probe play` and `bm-probe impair` — hand it a `BufWriter`, whose `Drop` flushes with the error discarded by design. So a capture truncated at the end, which is the part carrying the stall and error counters, was reported as written, and the error handling already sitting at each of those sites could not see it; it flushes and propagates now, which is what makes those branches reachable. The unit rows pin the contract rather than the filesystem state that provokes it, which is not reproducible in-process: a sink that fails only on flush, the same behind a buffer sized past the whole capture so the flush is the only write that ever reaches it, and an empty capture, since a flush conditional on having rows would leave the header-suppressed append path uncovered. The end-to-end half needs a target that accepts an open and then refuses every write, so it is a Linux lane. Measured 2026-08-18: nine seconds at a 2 s sampling interval is 1 946 bytes, inside one buffer, so nothing spills early and the flush is the entire file — to `/dev/full` the run reports `No space left on device` and exits 1, where the same build without the flush printed the path and exited 0. Only the `play` site is covered that way; the engine sampler and `impair` inherit the fix from the one function all three move their writer into. One thing the fix does not reach: `File`'s own close is still unchecked, which is a durability question rather than this one. The engine's own report goes through the host log sink now (see the diagnostic log sink row), so a Unity host does surface it — that was the other half when this row was written and is no longer | `cargo test -p media-diag` (in CI). By hand on Linux: `bm-probe play fixtures/h264-aac-320x180-30s.ts --duration 9 --interval-ms 2000 --csv /dev/full`; control is the same run to a writable path, which exits 0 with a 7-line CSV |
| Impairment run owns its capture | a capture that did not land fails the run. `bm-probe impair --csv` logged the failure and carried on to its grade and its normal exit status, so a run that lost its artefact reported IMPAIR PASS and exited 0 and a harness keying off that status kept it. The verdict is folded rather than returned early, so the grade still prints — the run did happen and its numbers are worth reading even when the CSV behind them did not survive. `play` already treated the same failure as fatal. Measured 2026-08-18 at the CI profile: writable target grades IMPAIR PASS, exits 0, 248 rows; target pointed at a directory reports the error, grades IMPAIR FAIL and exits 1, against IMPAIR PASS and 0 before the change | `bm-probe impair fixtures/h264-aac-320x180-30s.ts --profile ts-rtt300-loss005 --duration 25 --depth-ms 3000 --csv <an existing directory>` |
| Diagnostic log sink | every textual diagnostic the engine emits goes through one place, so where they land is the host's choice rather than fifteen hard-coded `eprintln!`s. `media-diag` holds a process-wide sink defaulting to stderr and a host installs its own as it loads; the crate stays platform-free and unsafe-free, so the routing is a unit row rather than something only a device can show. That row installs a capturing sink and asserts delivery, the macro's formatting and that a later install replaces the earlier one. It is one row rather than three because the sink is process-wide, so separate rows would race each other under the harness, and the default branch is the one no in-process row can read, since capturing the process's own stderr needs the platform. Naming the source is the sink's business rather than each site's: the default adds `[basis-media] ` for the console it shares with the host, an installed sink is handed the bare line because a platform log identifies itself by tag. Desktop output is unchanged as a result, which a run tripping the session-error path confirms. Fifteen sites across five crates — the session error, the reconnect ladder, the RTSP and RIST transport choices, the WHEP negotiation and its blocked candidates, both decode-route fallbacks, the DXVA manager refusal and the diag-CSV write. A Windows player has the same blind spot reached a different way — a GUI-subsystem process holds a stderr handle nobody reads — so its sink writes `OutputDebugStringW` as well as stderr, keeping the console a shell or a test run does have; one duplicated line where both channels exist costs less than silence where only one does. That half is exercised rather than structural: with a DBWIN reader attached, opening a session on a path that does not exist puts the engine's `session error:` line on OutputDebugString, and the same run with the install disabled puts nothing there while stderr still carries it, which is the shipped player's position exactly | `cargo test -p media-diag` (in CI); the row is load-bearing rather than self-satisfying — making `log` drop an installed sink instead of calling it fails that row and only that row, six of seven still passing. Desktop text unchanged: `bm-probe play fixtures/does-not-exist.mp4 --duration 3` prints `[basis-media] session error: …`. Windows channel: with DebugView running (or a short DBWIN_BUFFER reader), load the built `basis_media.dll` and call `bm_session_open` on a path that does not exist; control is the same run with `host_log::install()` disabled |
| An event drain is a backlog, not a loss | `bm_session_drain_events` hands back at most `cap` events and leaves the rest queued, so a caller whose buffer is smaller than the burst calls again rather than losing the difference. It used to empty the log with `take_events` and write only the first `cap` of what came back, discarding the remainder inside the call — silently, since `events_dropped` counts only what the ring's own cap refused. The managed drain asked for eight at a time against a 1024-event log, so any frame that produced more than eight lost the rest, and a session open and a transport loss both burst well past eight. That is exactly the moment the log is worth reading, and the ABI's own doc comment claimed the opposite behaviour. `take_events_up_to` on `SessionDiag` is the bounded form; `take_events` stays for the whole-log consumers | `cargo test -p media-diag` (in CI) — `a_bounded_drain_leaves_the_rest_queued` is load-bearing: putting `take_events`'s whole-log drain back inside it fails that row and only that row, nine of ten still passing (measured 2026-08-31, `left: 5, right: 2` on the first call's count) |
| Refused events are reported, not just counted | `SessionDiag::events_dropped` carried the comment "visible, never silent" and no export exposed it, so it was silent in practice: a session whose log filled handed over a sequence with holes in it and nothing said so. It reaches the ABI snapshot as `events_dropped`, taking the second reserved slot at offset 84 so the struct is unchanged at 88 bytes and the ABI stays 4. The engine counts in `u64` and the field is `u32`, so it saturates — a wrap would report a smaller loss than happened, and a small loss is what a reader discounts. Same stale-plugin caveat as `av_offset_us`: an old binary writes 0 there, which reads as "nothing lost" | `cargo test -p media-ffi --lib` (in CI) — `a_counter_past_the_snapshot_field_saturates` is load-bearing: replacing the saturation with a bare `as u32` fails that row and only that row, three of four still passing (measured 2026-08-31, `left: 0, right: 4294967295`). Layout: the 88-byte assert. End to end, by hand: drop `SessionDiag::default`'s cap from 1024 to 2, play any lane, and the Console warns `diagnostics log full: N event(s) refused`; restore |
| Unity host autotest | ABI v2 + managed component v2 end to end: presents via render event, audio on the Unity audio thread, mid-run seek | see "Unity host autotest" below |

## Not yet run

Surfaces that exist and have never been exercised. Each is a row in waiting, not
a known-good one.

| Row | What it would prove | How to run |
| --- | --- | --- |
| SEI user data — managed delivery, seek and loop | a seek drops the managed queue, and a loop (backwards PTS past the reordering slack) drops what the old pass left queued rather than parking it in front of the new one — unconditionally, since a loop restarts into the same timestamps the old pass opened with. The ordered-delivery half of this row has run (see the Demux table); these two transitions have not | a scene script subscribing and logging `ptsUs` against `fixtures/h264-sei-userdata-640x360-30fps.ts` served over HTTP: seek back and confirm the frame indices restart from the landed frame with nothing from the old position in between; loop the file and confirm the same across the wrap |
| RIST on Android — device pass | mr.town `rist://mr.town:5000` (plain) and `rist://mr.town:5001` (AES-128), matching the desktop rows above. Needs a Quest client build, so it is a device session, not a desk one. Run the stream preflight first and take the lanes down after — they are on-demand | the media pass on a connected Quest |
| Android audio geometry refusal — device pass | the Android audio adapter passes the container's stated rate and channel count into `AMediaFormat`, narrowing each to the `int32_t` the NDK takes; above `i32::MAX` that flips the sign, so a stream stating 3 GHz configures a vendor codec with a negative rate. Both are now bounded before the boundary and the adapter returns its typed refusal instead. The output-reading half was split out at the same time so the granted buffer index is released on every path including the refusals, since a codec whose output pool is not returned stalls, which reads as a hang rather than as the error that caused it. The geometry the codec reports live gets the same bounds the configured one does, since it overrides it and reaches the ring the same way — and the downmix-defeating output-channel ceiling asks for that same bound rather than a larger one, since a codec honouring a larger request would report a count the adapter then fails the track over. That half now also trims its payload to whole interleaved frames, the guarantee the software PCM adapter already makes: the chunking below it would drop a sub-sample tail silently, and a sample count short of a frame reaches the ring as a remainder it never accepts, parking the chunk until the inert-consumer discard clears it — a stall per malformed buffer, where trimming costs the same bytes and no stall. What is proven today is only that it compiles and lints clean for `aarch64-linux-android` — the guard sits in front of the NDK call, so nothing off-device can exercise it. The device row would play a Matroska file whose stated rate is out of range and confirm the audio track mutes with the refusal in the log while video plays on. Its sibling fix — proving the output-buffer window before slicing it, where a negative offset used to sign-extend and wrap the bounds check — is not reachable by any stream at all: the buffer info comes from the codec, so triggering it needs a misbehaving vendor blob. That one is an unsafe block's contract rather than a testable row, and is recorded here so its absence from the matrix is deliberate. The live bound reaches the right site now: `read_output` held the codec's live format to those limits, but `output_format` returned it unchecked, and that is the one the pipeline calls — to size the ring and to publish the session's audio geometry, both before any buffer arrives for the refusal to catch. It falls back to the configured pair, which is bounded at construction | on a connected Quest, `bm-url.txt` pointed at a crafted Matroska lane, `run-package-pass.ps1 -SkipBuild` after `tools\stage-android-plugin.ps1` |
| FFI callback panic fences — device pass | Every callback this engine hands to foreign code now absorbs a panic rather than unwinding into a frame that cannot take one: the four MediaCodec async trampolines on libmediandk's thread, the five Vulkan interception hooks on Unity's graphics thread and on the loader's, and librist's data callback on its own output thread. The workspace ships `panic = "unwind"` deliberately, because the ABI's own fences depend on it, so an unwind out of any of those is undefined behaviour on older toolchains and a process abort on current ones — the whole client, not the session. Alongside the fences, no lock reachable from one of those frames panics on poisoning any longer: the callback state, the Vulkan context and hook state, the librist ring, the renderer's graveyard and the engine's thread handles. Every one of those guards a value that stays structurally valid across an unrelated unwind (a queue, a handle vector, a byte ring whose head and fill move together), and recovery is what closes the actual path — the ABI fence catches a render-path panic without clearing the poison it left behind, which is how the device-event callback became reachable in the first place. The graveyard goes further than tolerating it and destroys its due entries after releasing the guard, so a destructor has no panic site left at all. The create hooks stopped asserting their own state too: a missing real function refuses, and a missing gipa costs the two probes behind it, both of which read learning nothing as leaving the caller's device alone. The codec's error-detail string is read through a bounded scan rather than a walk to whatever terminator it did or did not write, so neither how far that thread reads nor how much it allocates is the vendor blob's choice. What is proven today is that the whole `aarch64-linux-android` graph compiles and lints clean and that the desktop suite stays green. Nothing off-device drives a vendor codec into an asynchronous error or makes Unity's loader call a hook out of order, so these are structural rather than exercised. A caught panic is recorded as well as absorbed. The fence keeps the unwind off the framework's thread, which is its job, but discarding the result left the codec holding a granted index nothing would collect and the session waiting on a buffer that never arrived — a stall, with nothing said about the cause. It now goes where the codec's own asynchronous errors go, so the next use fails the session with a message. A device row would confirm the ordinary Quest package pass is unchanged — every one of these paths runs per frame, so a regression is not subtle — and that a session closed mid-render still tears down without leaking its Vulkan objects | on a connected Quest, `tools\stage-android-plugin.ps1` then `run-package-pass.ps1`; the desktop half rides `cargo test --workspace` |
| MediaCodec frame geometry refusal — device pass | `acquire_image` checks the status of every NDK call it makes and turns a failure into a typed `DecodeError` — bar the two that fetch the frame geometry, whose status was dropped, leaving the locals at zero and publishing that as the frame's size. Nothing downstream could tell: the present pass clamps the extent up to one before importing and then derives the sampling rectangle from what it imported, so the convert pass reads the wrong region of an otherwise valid hardware buffer and the viewer gets a garbled picture on a session still reporting Playing. Both statuses are checked now and a non-positive dimension is refused rather than clamped, which also makes the enclosing `// SAFETY:` note's claim that the out-params are checked before use true. What is proven today is that it compiles and lints clean for `aarch64-linux-android` and that the desktop suite stays green; the guard sits in front of an NDK call whose failure nothing off-device can provoke, so there is no automated row and the device row can only confirm the ordinary package pass is unchanged — the query runs per frame, so a regression is not subtle | on a connected Quest, `tools\stage-android-plugin.ps1` then `run-package-pass.ps1` |
| Vulkan present-layer lifetimes — device pass | three Android-only changes that compile and lint clean for `aarch64-linux-android` and cannot be exercised anywhere off-device. The destination image view was cached per Unity output image, keyed on the raw `VkImage` handle value — and Vulkan lets a driver give a later object the same value once the first is destroyed, so a `RenderTexture` the managed side recreated could land on a cached view over freed device memory and the compute dispatch would write through it. It is keyed now on a registration counter the managed side advances on every `bm_session_set_output_texture`, which is an identity no driver can recycle, with the previous view going back through the frame-number queue rather than being reused or destroyed in flight. Separately, the Unity interface tables' function slots are declared `Option`, so a slot a Unity build leaves empty is a value the code can test for rather than one that was invalid the moment it was materialised — which the existing null-device check already implied was possible, from a line that runs too late to help. And the device-event log line no longer formats the `VkDevice`: it is a dispatchable handle, so `{:?}` printed a live address from the client's heap into logcat on every launch. The device row would confirm the ordinary package pass is unchanged (the view cache sits on the per-frame path, so a regression there is not subtle), that a session closed and reopened against a fresh `RenderTexture` still presents, and that the log carries no address. The counter only holds if it is read with the pointer it belongs to, and it was not: registration stores the pointer and then advances the generation, so reading one of each in that order can hand the render event a new texture under the previous generation — the exact pairing the counter exists to exclude, since a recycled handle value then matches the cached view and the convert pass writes through it. They are read as a pair now, the generation taken either side of the pointer and the frame skipped where it moved, which costs one frame per registration. That predicate has a row of its own rather than a device one: the window is two adjacent stores wide and cannot be scheduled on demand, so the row drives the decision directly. The other half of the lifetime problem is that holding the `RenderTexture` past the close is not by itself enough. `graveyard::collect` runs from `SessionRenderer::render`, which a render event reaches only after finding a live session, and `bm_session_close` retires the handle in the same call — so a closing session's buried views are destroyed by some other live session's render events, and with one player and nothing else open there are none. However long the managed side waits, releasing the texture then destroys the image under a live view. The collector now has an entry that needs no session, reached by issuing the render event with `BM_EVENT_COLLECT` instead of a session handle, which is what a caller holding a retired texture drives it with. It is a new event id on the existing render-event function rather than a new export, so the ABI stays at 4 and the export list at 26. The managed side keeps its half: `Close` hands the `RenderTexture` to a retirement queue that issues sixteen collect events before releasing and destroying it, rather than releasing in the same breath as the close. Sixteen because it has to exceed the deepest frames-in-flight Unity runs, which is the same reason the present layer sizes its descriptor ring there. The queue counts events issued rather than seconds elapsed, so an app that stops rendering holds its textures until it renders again — the same bound the plugin's own parking has, and releasing on a timer would destroy exactly the image the wait exists to protect. It outlives the player's GameObject, since a close is routinely followed by that object going away. The replace path goes through the same helper as the close, though nothing can reach it today: the texture is only ever created where there is none. Both halves compile for their own graph and neither is reachable from any test — the Rust drain needs a Vulkan device and the managed queue needs an Android player build, which is why the editor compile alone says nothing about it (checked here by compiling the runtime assembly a second time under the player's defines). The device row is what says this works, and the case it is for is closing the *last* player: no validation complaint, and the next open still presents | on a connected Quest, `tools\stage-android-plugin.ps1` then `run-package-pass.ps1`; then `adb logcat -s basis-media` for the log half |
| Vulkan device-creation feature guard — device pass | the interception hook forces `samplerYcbcrConversion` on into Unity's own `vkCreateDevice`, which is the feature the AHardwareBuffer import path samples through. Asking for a feature the physical device does not advertise fails that create with `ERROR_FEATURE_NOT_PRESENT`, and the device belongs to the app rather than to the video path, so the cost of assuming it was the whole client. It is probed now, through the same instance gipa the extension list already goes through, and left as the caller set it where the driver does not advertise it or where the probe cannot run at all — a 1.0 instance without `VK_KHR_get_physical_device_properties2` can be asked neither spelling of the features query, and calling a name the instance never supported is not defined however willingly a loader hands the pointer over, so which spellings are usable is decided at instance creation where the API version and the enabled extensions are visible, while which of them to put first is decided per physical device: the version an instance was created at is not the version the driver behind a given device implements, and `vkGetPhysicalDeviceProperties` is core 1.0 so the device can always be asked. The other name is still tried behind the preferred one. Both are kept rather than one chosen there: the version the application asked for is not the version the driver behind a given physical device need implement, so an instance created at 1.1 handed a device from a 1.0 driver had the core name committed to and the extension spelling never tried. The probe takes whichever the loader resolves, and still offers it no name the instance did not support. The chain walk that sets the feature is bounded at sixty-four hops, far past any real chain: the chain is the caller's, and a cycle in one this hook does not own would spin inside `vkCreateDevice` on the app's own thread, where the panic fence is no help. The bound gives the walk a third way to finish, though, and only one of the three proves the caller chains no such struct — so the feature is prepended where the walk reached the terminator and nowhere else. Stopping at the cap and prepending anyway would put two of the structs the spec allows only one of into the chain, which fails the create for the app's own device: the same failure the probe exists to avoid, arriving by the bound meant to make the walk safe. The interfaces are also published before the device-event callback is armed rather than after: registering can dispatch the initialize event from inside the register call, and the handler gives up where the Vulkan interface is absent — which it was. That event is the documented and only point at which the instance becomes available and it does not come again, so losing it left the present path inert for the run. What is proven today is that the `aarch64-linux-android` graph compiles and lints clean; nothing off-device creates a Vulkan device through the hook. Quest advertises the feature, so the device row is a no-regression check rather than a demonstration: the ordinary package pass still shows video, and the log carries the appended-extension line without one saying samplerYcbcrConversion was left as the caller set it. That line names its cause, because the probe answering a bare bool made a driver that does not advertise the feature and a probe that could not run at all look identical — and they are different places to look, the first being the driver's answer and the second saying the interception had nothing to ask with | on a connected Quest, `tools\stage-android-plugin.ps1` then `run-package-pass.ps1`, then `adb logcat -s basis-media` |
| Engine diagnostics on device — logcat pass | the sink `host_log::install()` puts in place at `JNI_OnLoad` points every engine diagnostic at logcat under the `basis-media` tag, which is the tag the present layer already writes to, so the engine's account of a session and the present layer's arrive interleaved in one stream. `JNI_OnLoad` rather than `UnityPluginLoad` because it is the first moment the library runs, does not depend on the preload flag the Vulkan interception needs, and is unconditional, so no diagnostic can precede the sink being in place. Before this the lines went to stderr, which Android gives a native process no readable copy of, so the engine's whole textual account of a device session was discarded — on the platform where a session is hardest to observe and where the capture CSV is only readable after the fact. What is proven today is that the `aarch64-linux-android` graph compiles and lints clean and that the desktop suite and the sink's own rows stay green; nothing off-device installs the Android sink, so the routing is structural there rather than exercised. The device row would confirm engine lines actually appear, for which a deliberate failure is the cheapest check — `session error:` fires on a URL that does not resolve — and that a session which plays normally shows the transport line for its lane | on a connected Quest, `tools\stage-android-plugin.ps1` then `run-package-pass.ps1`, then `adb logcat -s basis-media` |
| Proton / Wine (standing row) | the inference is only as good as the headers it reads, and the C player's equivalent broke wholesale under Wine — its Content-Length query used `WINHTTP_QUERY_FLAG_NUMBER64`, which Wine omits from its query modifier mask, so every source looked non-seekable and every VOD fast-forwarded for Linux users. This engine goes through reqwest so it does not inherit that bug, but has never been exercised there. Two things to check rather than assume: (1) the 206 probe, `Content-Range` and `Content-Length` all read correctly under Proton, so the same URL classifies the same as on Windows; (2) https works at all — Windows builds use rustls with *native* roots, which under Wine means Wine's certificate store, and an empty one fails every https source before liveness is even reached (Android needed bundled roots for the same class of reason) | the Windows client under Proton on a Linux box, one ranged VOD and one live HTTP-TS lane |

Two more sit inside the Android section rather than here, because their rows
carry the detail: the https-on-device lane (the bundled trust anchors have never
been confirmed on hardware) and the render-event re-grade (verified headless and
in the autotest, never re-graded from a device hold histogram).

The managed side's un-run work — shared playback with two clients, the session
cap in a populated scene, the panel, the resolver's first real run — is listed in
[`../TESTING.md`](../TESTING.md). None of it has an automated row either.

## Rig limitations

Fixtures and feeders that are wrong for a test, as opposed to the player being
wrong. Recorded so the next session fixes the rig instead of redesigning the
player around it.

| Limitation | What it does | How it shows |
| --- | --- | --- |
| Single-slot live feeders | the mr.town `:8093` and `:8094` lanes are ffmpeg `-listen 1`, one client per slot with a systemd respawn, so the liveness probe's ranged GET takes the only slot and the handover connect is refused — measured 2026-08-16: the slot was still refusing a full second after the probe closed. This is the fixture being wrong for the test, not the player: every real live HTTP server (VRCDN, nginx, a CDN edge) accepts concurrent clients, and VRCDN infers correctly. Until those feeders grow a fan-out relay, drive them with `liveness = Live` set explicitly, and use VRCDN for anything exercising the inference | `bm-probe play http://mr.town:8094/imax.ts` on defaults fails with a connect error; with liveness forced Live it plays as it always has |

## Android — the device matrix

MediaCodec decode and the Vulkan present path, on Quest Pro. These are device
rows: none of them run in CI, and each states its own verdict and date.

| Row | What it proves | How to run |
| --- | --- | --- |
| Android build lane | the whole engine graph (media-ffi cdylib, decode-mediacodec, the Vulkan present module, decode-sw's Opus/FLAC floors cross-compiled via cmake+NDK) compiles clean for `aarch64-linux-android` with clippy `-D warnings`; the AV1 software floor is deliberately absent from this graph (rav1d's published crate cannot build its arm64 asm, and the present path has no CPU-frame upload) | in CI (`android check (aarch64)` row; skips loudly without an NDK). By hand: `. .\tools\android-env.ps1` then `cargo clippy --target aarch64-linux-android -p media-ffi -p decode-mediacodec -- -D warnings` |
| Engine .so | `libbasis_media.so` links against exactly `libmediandk`/`liblog` (+ libc/m/dl) and exports the ABI plus `UnityPluginLoad`/`JNI_OnLoad` | `. .\tools\android-env.ps1; cargo build --target aarch64-linux-android -p media-ffi --release`, then `llvm-readelf -d`/`--dyn-syms` on `target/aarch64-linux-android/release/libbasis_media.so` (the M0 DT_NEEDED lesson) |
| On-device pass (Quest) | the M5 vertical slice on hardware: MediaCodec async decode (hardware AVC) → AImageReader → AHardwareBuffer → Vulkan import on Unity's device → compute convert into a Unity RenderTexture under OpenXR; audio via MediaCodec AAC through the ring; capability blob with hardware routes + MediaCodecList ceilings; mid-run seek (flush/restart in async mode); natural Ended | stage the `.so` into the package (`tools\stage-android-plugin.ps1`; the spike host loads it from there), then `UnityScratch\BasisMediaM0Quest\run-media-pass.ps1` — builds `bmmedia.apk`, deploys via adb, soaks ~25 s, prints the `BM_MEDIA_VERDICT` line (decode/present/ended/audio each PASS). **PASSED 2026-08-14 on Quest Pro** (repeat runs: ~300 decoded / ~275 presented, 0 errors, Playing at ~45 ms, seek + natural Ended; picture verified correct-side-up in stereo under URP; video decoder resolves to OMX.qcom.video.decoder.avc, audio to c2.android.aac.decoder). Known open item, root-caused: the harness's audio consumer pulls at ~half the hardware cadence, dragging the audio-master clock (~0.7 s snap-backs every ~1.5 s — play/freeze cycling); with the pull muted (`bm-no-audio` marker in the app's files dir, no rebuild needed) the engine paces exactly 1 s/s with zero snaps, so the engine is exonerated — the fix is the managed package's Android audio path (the row below). The OMX decoder also never delivers an EOS-flagged output (the bounded drain-timeout fallback ends the stream) |
| Managed package pass (Quest) | `com.basis.mediaplayer`'s Android arm end to end: the component owns the Vulkan graphics contract (linear RGBA32 RenderTexture with `enableRandomWrite`, render events via `RenderPipelineManager.endCameraRendering` — URP ignores camera command buffers) and the DSP-rate-aware audio pull (the ring is consumed at the *stream* rate and linearly resampled to the device DSP rate; a raw 1:1 pull consumes at dspRate/streamRate of real time and drags the audio-master clock — the raw harness's half-cadence class). The verdict grades decode/present/ended plus **audio** (pulled frames per wall second ≈ stream rate) and **pacing** (position advances 1 s/s with zero backwards snaps after the seek settles, sampled at 4 Hz) | `UnityScratch\BasisMediaM0Quest\run-package-pass.ps1` — the host project references the package as a `file:` dependency and the `.so` ships inside the package (`tools\stage-android-plugin.ps1` after any repo change); builds `bmpackage.apk`, deploys via adb, soaks ~35 s, prints `BM_PKG_VERDICT decode/present/ended/audio/pacing`. A/B without rebuild: `bm-no-audio` marker in the app's files dir removes the AudioSource (wall clock master); `bm-url.txt` (line 1 = URL, later lines `live`/`audio-lead`) drives any lane — URL runs skip the seek and the Ended gate; `bm-hold` delays the open 15 s (a no-session `dumpsys meminfo` baseline window for the per-session A/B); `bm-two` opens a second session on the same source beside the first (per-second `s2` lines + a `BM_PKG_S2` counter line in the log). Unattended runs (no wearer): `adb shell am broadcast -a com.oculus.vrpowermanager.prox_close` fakes the proximity sensor so the app renders headset-on-desk (`…automation_disable` restores it) — note the eleventh session's ideal-hold percentages were measured worn; desk runs grade ~90–95% ideal with zero skips, so compare like with like. **Steady state PASSED 2026-08-15 on Quest Pro, in-headset confirmed** (smooth playback, continuous tone; no-seek run: position exactly 1 s/s, 175/175 presented, audio pulled at the stream rate to the content-bound total, natural Ended, zero snaps; boot log confirmed the device DSP rate is **24000 Hz** — the raw harness's half-cadence consumption was exactly dspRate/streamRate). **The full verdict including the seek is green since the seek-settle fix (2026-08-15, same day)**: the seek settles with a 28 ms presentation gap (was ~5 s — the OMX drain blocking the flush, a stale-frame clock restart, and the ring free-running through the settle), lands at the keyframe, 1 s/s after, natural Ended with the tail intact. The audio-led slowjoin lane still fails the pacing gate on its single join-window snap — the documented audio-led join shape (the audio-leading-start trade), not a defect; the gate is not join-aware on URL runs |
| Android https | TLS trust anchors on Android come from the bundled webpki-roots CCADB set (no readable OS CA store there — the first device https attempt failed at connect with error 103; every other platform keeps the OS store): an `https://` lane connects and plays on device | **PENDING first device run** (code landed 2026-08-15): rebuild + `tools\stage-android-plugin.ps1`, `bm-url.txt` = `https://mr.town/vod/music_51.flac` (bring `tls443` up), `run-package-pass.ps1 -SkipBuild` |
| Diagnostics captures | two layers, both CSV: (1) engine-side — `diag_csv` in the descriptor (managed `diagnosticsCsvPath`) makes an engine-owned thread sample the §12.4 capture recorder at 100 ms and write it on close, same pinned column contract as `bm-probe play --csv` (`cargo test -p media-engine --test session diag_csv_written_on_close` pins the surface); (2) display-side — the Quest package harness writes `bm-frames.csv`, one row per Unity frame (wall_dt, state, position, banked, decoded/presented, audio pulled — the C player's judder-relevant columns). The engine CSV sees flow/starvation; only the frame CSV sees display cadence (hold intervals) — the 2026-08-15 judder analysis needed both | on device: run any package pass, then `adb pull .../files/bm-diag.csv` + `bm-frames.csv`; scratch analysis scripts from the session live outside the repo |
| Steady-lane cadence (Quest) | the master-observation filter holds presentation on the ideal 3-vsync hold through Quest's DSP-callback jitter episodes: post-join windows on the rtspt stereo lane grade 97.4–100% ideal holds with **zero newest-wins skips** across repeat soaks (pre-fix: ~74% with ~2 skips/s during episodes). One caveat when reading the histogram: the mediamtx stereo lane's re-encoder drops the odd frame (~one 83 ms pts gap per 20 s, ffprobe-verified) which displays as a correct 6-vsync hold. The residual 1–5 isolated one-vsync-late/early hold pairs per ~25 s (presentation-handoff quantisation) are what render-event selection targets: frame selection now runs in the render event itself with a vsync of lookahead, so those pair events should collapse to isolated occurrences minutes apart — **the on-device re-grade of this row on a render-event build is pending** (headless + autotest verified; the histogram is the device verdict) | bring the stereo lane up, `bm-url.txt` = `rtspt://mr.town:8090/imaxstereo` + `live`, `run-package-pass.ps1` (full build — the staged `.so` must be the render-event build), pull `bm-frames.csv`, grade hold intervals over a post-join window (scratch script; rewrite from the column contract) |
| §11 budgets on device (first measurements, 2026-08-15, Quest Pro, 640×360 A/V fixture) | startup → first frame: decode and present both inside the first 100 ms sampler bucket of the engine capture (budget: ≤ C player −20%; the C-on-Quest baseline still needs its own run). Seek → settled: **28 ms presentation gap** (last pre-seek present to first post-seek present in the frame capture; was ~5 s before the seek-settle fix, and matches Windows' ~26 ms). Binary size: **inside the reset budget** — the §11 row is ≤12 MB per platform (reset from 8 MB, 2026-08-15): `libbasis_media.so` 9.2 MB (was 15.0; fat LTO + codegen-units 1 + staging strip-all) / `basis_media.dll` 10.7 MB (was 11.8; MSVC keeps debuginfo in the .pdb, so the dll is essentially all code). The remainder is the feature set's weight — the `cargo bloat` audit shows a long tail (std 1.5 MiB, rav1d 0.7 MiB, the WHEP stack ≈1.2 MiB including its unused SCTP, rustls 0.4 MiB), with the str0m SCTP feature-gate upstream PR as the named first move if size ever binds. Per-session memory (2026-08-15, twelfth session, `bm-hold` no-session baseline A/B): app no-session baseline ~310 MB PSS / EGL ~27 MB; one 640×360 session ≈ +21 MB PSS; one 1080p30 session ≈ +70–77 MB PSS with **EGL +53 MB** (RGBA RT + the 10-image AImageReader pool — inside the ≤80 MiB decoded-side row, first crude measurement); two 1080p30 sessions EGL +100 MB (≈50 MB/session, linear). Two simultaneous sessions (`bm-two`): **both full rate at 1080p30** — session 1 all gates PASS (900/896 @30 s, ratio 1.000, audio at rate), session 2 900/895 at 1 s/s with full audio; app CPU 37% of one core for one 1080p session, 44% for two (hardware decode keeps it flat). Unmeasured rows (per-stream CPU vs the C player, main/render-thread cost, C-on-Quest baselines) need C-player A/B runs | fixture package pass + mid-soak `adb shell top` / `dumpsys meminfo`; TTFF/seek from `bm-diag.csv` / `bm-frames.csv`; the 1080p lane is a generated fixture (ffmpeg testsrc2 1080p30 H.264 high + AAC, 60 s, timecode burn-in) pushed to the app files dir as `bm-1080p.mp4` and driven via `bm-url.txt` |
| Live lanes on device (Quest) | the M5 live matrix over the package pass's `bm-url.txt` lane driver — **all run 2026-08-15 on Quest Pro, decode/present/audio green on every lane**: RTSP stereo (`rtsp://mr.town:8090/imaxstereo`, 655/651, ratio 1.000, in-headset smooth) · RTSP 5.1 (`…/imax51` UDP + `rtspt://` TCP, audio at rate through the managed stereo downmix — the downmix itself is listening-verified on the clean FLAC 5.1 VOD lane; the imax51 lane additionally pops and degrades over a soak on ANY build because its `-c copy` passthrough carries millisecond-quantised timestamps whose pts timeline runs slower than the sample count, saturating the PcmRing — reproduces headless on Windows, kept as the ring-serve repro lane; per-speaker mapping stays the managed multichannel work) · WHEP (`whep://mr.town:8091/whepav/whep`, **first Android WHEP run** — join <2 s, ratio 1.000, Bank grinding to the sub-second floor) · HLS live (local windowed server via `adb reverse tcp:8899`, wrap splices handled; the pacing gate's "snaps" are the documented splice timeline restarts, expected FAIL on this lane). Visual A/B: occasional macroblock pixellation on the UDP-carried lanes (RTSP-UDP, WHEP) heals at the next keyframe and vanishes on the TCP twins — transport loss, not the present path (WHEP NACK/PLI is unbuilt); no FOREIGN-barrier-class artefacts seen; the OMX drain-timeout stayed bounded throughout | bring the mr.town lanes up per the remote-servers notes (+ the transient `whepav` publisher), preflight, then per lane: write `bm-url.txt`, `run-package-pass.ps1 -SkipBuild` |

Trust anchors on Android: there is no readable OS CA store, so the TLS stack
bundles the webpki-roots CCADB set there while every other platform keeps the OS
store. The code landed 2026-08-15; the device run that confirms an `https://`
lane connects and plays is the pending half of the row above.

The Vulkan graphics contract (normative, mirrors the D3D11 one): a
**linear** (sRGB off) RGBA32 RenderTexture with `enableRandomWrite`,
display-sized, registered via `bm_session_set_output_texture`; the plugin
preloaded so the Vulkan-init interception registers before graphics init
(the package's committed `.meta` sets `isPreloaded`); render events issued
per frame as on D3D11. `BasisMediaPlayer` implements the contract on
Android — the raw harness stays as the A/B reference consumer.

## Measurements

Where the healthy bands above come from. Dated, because they are readings rather
than rules, and a later reading supersedes an earlier one on the same lane.

### Bench baseline (2026-08-14)

The §11 budget comparison against the C player measures from these
numbers (medians of 3 via `bm-probe bench`, Windows dev host; raw
per-run spread from the same sweep is reproducible per the Bench lane
row above):

| Lane | TTFF | Seek-to-settled |
| --- | --- | --- |
| MP4 faststart (file) | 134 ms | 30 ms |
| MP4 trailing moov (file) | 135 ms | 28 ms |
| MP4 fragmented (file) | 133 ms | 27 ms |
| MPEG-TS (file) | 131 ms | refuses by design |
| MKV (file) | 130 ms | 35 ms |
| HLS-TS VOD (local files) | 124 ms | 23 ms |
| HLS-fMP4 VOD (local files) | 134 ms | 28 ms |
| HLS live (local windowed server) | 195 ms (was 1.15 s pre-priming) | — |
| MP4 ranged (mr.town HTTPS) | 327 ms | 35 ms |
| HLS-TS VOD (mr.town HTTPS) | 350 ms | 19 ms |
| HLS-fMP4 VOD (mr.town HTTPS) | 308 ms | 15 ms |
| RTSP mediamtx stereo (live) | 4.39 s | — |
| RTSP mediamtx slowjoin (adversarial ~10 s GOP) | 8.08 s | — |
| RTSP VRCDN (live) | 2.02 s | — |

RTSP-over-UDP joins (2026-08-14, same method — `rtsp://` negotiated
onto UDP; same-day `rtspt://` comparisons in brackets):

| Lane | TTFF |
| --- | --- |
| RTSP-UDP mediamtx stereo (live) | 4.00 s [TCP same day: 4.06 s] |
| RTSP-UDP mediamtx 5.1 (live) | 6.25 s |
| RTSP-UDP mediamtx slowjoin (adversarial ~10 s GOP) | 7.64 s |
| RTSP-UDP VRCDN (live) | 3.96 s [TCP same day: degraded/variable — one 14 s join, one 30 s timeout; the 2.02 s baseline row was a healthier day] |
| HTTP-TS VRCDN (live) | 1.97 s |

RIST joins (2026-08-14, same method — mr.town broadcasters, 16 Mbps
x264 + AAC 5.1, 2 s GOP, WAN):

| Lane | TTFF |
| --- | --- |
| RIST plain (mr.town :5000, live) | 1.19 s |
| RIST AES-128 (mr.town :5001, live) | 1.23 s |

WHEP joins (2026-08-14, same method — mediamtx `whepav` path, H.264
`-bf 0` + Opus stereo, 2 s GOP; join = signalling + ICE/DTLS + keyframe
wait, so the GOP dominates the spread):

| Lane | TTFF |
| --- | --- |
| WHEP (mr.town :8091, WAN) | 2.57 s median / 1.06 s min / 3.02 s max |
| WHEP (local mediamtx) | ~1.0–2.2 s (a many-interface host offers many ICE candidate pairs and nomination occasionally settles on a dead one, stalling that session; the reconnect path recovers. Single-candidate WAN topologies did not show it) |

Decode-breadth lanes (2026-08-14, same method; audio-only TTFF is
time-to-first-audible-audio):

| Lane | TTFF | Seek-to-settled |
| --- | --- | --- |
| VP9+Opus WebM (file) | 126 ms | 33 ms |
| AV1+Opus WebM (file, rav1d) | 123 ms | 673 ms (decoder refills its frame pipeline) |
| FLAC / MP3 / ADTS / Ogg Opus (file, audio-only) | 22–29 ms | refuse by design (no seek tables built) |
| VP9 WebM 1080p (mr.town HTTPS) | 307 ms | refuses (MKV) |
| AV1 WebM 1080p (mr.town HTTPS, rav1d) | 399 ms | refuses (MKV) |
| AV1+AAC MP4 1080p60 (mr.town HTTPS, rav1d) | 328 ms | 36 ms |
| AV1 fragmented MP4 (mr.town HTTPS) | 1.5 s | 37 ms |
| FLAC / MP3 / Ogg Opus (mr.town HTTPS, audio-only) | 148–162 ms | refuse |

The two audio-only "refuse" rows are as measured on the day. Every raw-audio
lane seeks since 2026-08-16 — see [Raw-audio seek,
restored](#raw-audio-seek-restored-2026-08-16) for MP3 and Ogg Opus, and
[FLAC and ADTS seek](#flac-and-adts-seek-2026-08-16) for the rest.

Reading: the WebM/MP4 codec lanes match the container-independent
~130 ms local / ~300–400 ms remote pattern. The fragmented-MP4 outlier
is open cost (the box walk visits every moof, spread across the file —
each a cache-block fetch over HTTPS). tos_vp9.mp4 seeks settle
inconsistently (its muxing marks samples sync that are not — the landed
"keyframe" can sit mid-GOP); MKV seek stays refused pending the
matroska-demuxer fix or our own cue walker.

Reading: VOD TTFF is container-independent (~130 ms locally; network
lanes add ~one fetch round trip), seeks land in 15–35 ms everywhere
they are supported, and the two VRCDN transports join within 50 ms of
each other. The mediamtx joins carry their lanes' GOP structure (2 s
and ~10 s respectively) plus the live hold — the adversarial lane's
join cost passes through undamped, as it is designed to.

### Live-join baseline after the priming join (2026-08-14)

Same-day before/after, medians of 3 via `bm-probe bench --live`
(before = pre-priming build, same host, lanes up for both passes):

| Lane | Before | After | Reading |
| --- | --- | --- | --- |
| HLS live (local windowed server) | 1154 ms | 195 ms | backlog-burst lanes are where priming wins: the 3-segment join primes the decoder at line rate instead of 1x |
| RTSP-UDP mediamtx stereo | 3725 ms | 2689 ms | the SR-aligner's ~2 s join burst now primes unmetered |
| rtspt mediamtx stereo | 3946 ms | 2082 ms | same mechanism |
| RTSP-UDP mediamtx slowjoin | 5652 ms (4.4–9.3 s) | 8648 ms (8.2–10.5 s) | undamped by design: the join cost is the lane's content-dependent IDR spacing (~10 s GOP with scene cuts), and both spreads sit inside it |
| rtspt mediamtx slowjoin | 10330 ms (8.8–10.5 s) | 5289 ms (2.1–5.7 s) | same caveat — IDR-phase luck dominates run-to-run |
| WHEP (mr.town, WAN) | 2648 ms | 1067 ms | GOP-dominated both sides; audio-first runs measure ~1.07 s on both builds; one 11.7 s ICE-retry outlier seen after |
| RIST plain / AES-128 | 1170 / 1236 ms | 1161 / 1186 ms | parity — arrival-bound 1x lanes have nothing to prime ahead of |
| VRCDN http-ts | 410 ms | 471 ms | parity (day noise; the 1.97 s baseline row was a slower day) |
| VRCDN rtsp | 3688 ms | 1977 ms | aligner-burst priming, as the mediamtx lanes |

Priming cannot beat arrival physics: a 1x push lane with no server-side
backlog joins no faster than its keyframe wait plus the decoder's
first-output input depth at 1x. The wins come from lanes where a join
burst exists (HLS segments, the RTSP SR-aligner's flush) that the old
1x-metered startup released in real time.

The per-track release + pre-join shed re-verified this table (2026-08-14,
same lanes, medians of 3): HLS live local 143 ms, rtspt stereo 2094 ms,
RTSP-UDP stereo 3172 ms (2.3–5.6 s spread), rtspt slowjoin 2948 ms and
UDP slowjoin 10531 ms (both inside the lane's ~10 s IDR spacing), WHEP
1717 ms (GOP-dominated), RIST plain/AES 1224/1278 ms, VRCDN http-ts
245 ms. VRCDN rtsp was degraded that day on both builds (the pre-change
build measured 11.2 s median on the same lane in the same hour; the new
build's healthy runs sat at 1.9–2.3 s over TCP) — no regression
attributable to the change. Publisher-restart rejoined on attempt 3
(924/924 presented, 0 drops); CI impair row + all full-length phase-0
profiles passed with margins unchanged and `audio_ring_drops` 0 on the
CI row (41 before the shed rework).

### Hardware-route measurements (2026-08-15, desk box, NVIDIA)

Release `bm-probe bench` on the A/V MP4 fixture, 5 runs: **hardware
route TTFF median 154 ms (min 53, max 244 — device + DXVA MFT setup
varies run to run), seek 15 ms; software route (via
`BASIS_MEDIA_DISABLE_HW_DECODE`) TTFF 126 ms, seek 5 ms.** The ~30 ms
median TTFF cost is the decode-device/manager creation; the payoff is
the CPU row below.

Per-stream CPU A/B (1080p30 H.264+AAC 20 s generated lane,
`bm-probe play --duration 20`, process CPU time): **hardware 0.75 s
(≈3.7% of one core), software 5.45 s (≈27%)** — a ~7× reduction; both
routes ~597/594 decoded/presented, 0 pool drops. The remaining §11
Windows rows (vs the C player on the same lane, VRAM per session with
the decoder pool now GPU-side) still need the C-player A/B pass.

### Raw-audio seek, restored (2026-08-16)

MP3 and Ogg Opus seeks were regressions against the C player, which seeked both. Medians
of 3 on the committed fixtures, after porting its arithmetic:

| Lane | Duration reported | ffprobe says | Seek settle |
| --- | --- | --- | --- |
| `sine-48k-stereo.mp3` | 6.024 s | 6.000 s | 3 ms |
| `sine-48k-stereo.opus` | 6.000 s | 6.007 s | 3 ms |

Both differences are expected and are the C player's conventions. MP3's comes from the
Xing frame count (251 x 1152 / 48 000), which is what the seek arithmetic is keyed on,
while ffprobe applies the encoder's delay and padding. Ogg's is the other way round:
ffprobe counts the priming samples, which never reach the output, and the reported
duration subtracts pre-skip exactly as the packet timestamps already do.

Two traps the Ogg path hit, both after a seek rather than during one: the reader holds a
half-packet across a bisection unless `delete_unread_packets` is called, and a bisection
towards the start re-reads `OpusHead`/`OpusTags`, whose first byte is not an Opus TOC.
Either one surfaces as a malformed-TOC parse error, not as a bad landing.

### WAV lane bench (2026-08-16)

Medians of 3. `bench`'s seek-settle gate required a video presentation, so it timed out on
*any* audio-only lane that seeks — MP4 audio (`.m4a`) has always been one, since
`Mp4Demuxer::seek` lands straight on the target when there is no video track. Nothing had
benched a seek on one until WAV arrived. The gate now falls back to the post-seek position
where a session has no video: `aac-48k-stereo.m4a` settles in 2 ms and `sine-48k-51.m4a` in
17 ms, so the fix is not WAV-specific.

WAV is the raw-audio lane that seeks most cheaply, PCM being linear.

| Lane | TTFF | Seek settle |
| --- | --- | --- |
| `fixtures/sine-48k-stereo.wav` (16-bit stereo, local) | 23 ms | 34 ms |
| `https://mr.town/vod/w.wav` (7.1 LPCM, 24.3 MB, HTTPS) | 135 ms | 4 ms |

The remote lane also pins the real-content half of the WAV work: 8 channels at 48 kHz, a
31.678 s duration derived from the byte rate, played through the PCM adapter.

### FLAC and ADTS seek (2026-08-16)

The last two raw-audio lanes that refused. Neither carries an index the MP3 and Ogg
arithmetic could be pointed at, so each got the mechanism its container actually affords.

FLAC frame headers state their own sample number, so the whole problem is finding a byte
position: a SEEKTABLE states one outright, and without one the position is bisected over
frame headers, each probe reading back the sample number that narrows the interval. The
probe interpolates between the interval's known sample numbers rather than halving it, but
is clamped to the middle half, so a mis-estimate still cuts the window by a quarter and the
search cannot stall. Because the landing is read back rather than guessed, FLAC reports the
exact instant playback resumes at — the only raw-audio lane that does.

A probe only accepts a frame header that the frame *following* it confirms, and headers are
now screened against the sample rate and channel count STREAMINFO announced. The CRC-8
alone leaves roughly one false sync per 256 structurally valid candidates, which never
mattered while the walk only ever started at the first frame and matters immediately once
it starts mid-file.

ADTS states neither a length nor a frame count, and its frames carry no timestamp, so both
its duration and its landing rest on the byte rate averaged over the leading 128 frames
(ffmpeg estimates the same way). The landing rounds down to a frame boundary — 21.3 ms at
48 kHz — and the timeline re-anchors on it. That estimated duration is the reason the seek
is reachable at all: without one there is no scrub bar to seek from.

Medians of 3 on the committed fixtures. The FLAC fixtures carry STREAMINFO, VORBIS_COMMENT
and PADDING but no SEEKTABLE (ffmpeg writes none), so these are all the bisection arm.

| Lane | Duration reported | ffprobe says | TTFF | Seek settle |
| --- | --- | --- | --- | --- |
| `sine-48k-stereo.flac` (~1.3 kB frames) | 6.000 s | 6.000 s | 23 ms | 3 ms |
| `sine-48k-71.flac` (~52 kB frames) | 6.000 s | 6.000 s | 9 ms | 3 ms |
| `sine-48k-stereo.aac` | 6.023 s | 6.031 s | 24 ms | 2 ms |

ADTS is the only estimated duration in that table, and ffmpeg estimates it the same way:
ffprobe reports 6.031 s for the file off a 98 608 bps bitrate estimate, against the 6.037 s
its 283 frames actually store. This lane's 6.023 s is the same calculation over a smaller
window, so both land short — ffmpeg by 6 ms, this by 14 ms, on six seconds. `RATE_SAMPLE_FRAMES`
is the knob: 32 frames gives 6.054 s, 128 gives 6.023 s, every frame in the file gives
6.037 s. Expect the error to stay small on CBR and widen on a file whose opening seconds
are not the rest of it.

The error is confined to the far end of the scrub bar. Position is counted in frames, not
estimated, so it stays exact until a seek — after which the landing carries whatever the
byte estimate missed, exactly as MP3's does.

**Measured over HTTPS (Editor pass, 2026-08-16).** A ranged source pays a round trip per
probe, which is what the interpolation exists to keep down. Against nginx with keep-alive,
on files that carry no SEEKTABLE so every seek runs the full bisection:

| Lane | Seek → landing |
| --- | --- |
| `https://mr.town/vod/music_16.flac` | 235–418 ms |
| `https://mr.town/vod/music_var.flac` (variable blocksize) | 202–366 ms |
| `https://mr.town/vod/music_51.flac` (5.1) | 199–305 ms |

Inaudible in all cases — the ring covers it, and the clock never dropped a sample (0 silent
output rows across every seek, ≤3-frame audio gaps, `frame_dt_ms` never above 34 ms while
playing). The seek does **not** block the caller: `bm_session_seek` posts to the demux
thread.

Two things this pass established that the fixtures could not:

- **A seek costs one reposition on ADTS and about four on FLAC**, so FLAC is four times as
  exposed to a slow source. Against a server with no keep-alive (~300 ms per read) rapid
  scrubbing starved the bank for **7.9 s** on FLAC and **25 ms** on ADTS over the same
  transport, the only variable being reads per seek. Against nginx the same scrubbing —
  seeks 0.58 s apart, faster than the starving case — never left Buffering for longer than
  19 ms. Not a defect, but the reason a SEEKTABLE matters: it cuts the seek to one read
  (measured 2.6× faster end to end on an injected table).
- **The 5.1 interleave survives a seek**: verified by ear with six discrete outputs bound,
  not through the stereo downmix, which sums the channels and would hide a rotation. The
  sine fixtures cannot show this at all — every channel carries the same tone.

## Harnesses

### Unity host autotest

The disposable host project (`C:\Users\Matt\Documents\UnityScratch\BasisMediaM0Host`,
Unity 6000.5.8f1, forced D3D11) references the `com.basis.mediaplayer`
package as a local file dependency and drives it in batch play mode
(commands from the workspace root, `Native~`):

```
cargo build --release -p media-ffi
copy target\release\basis_media.dll ..\Runtime\Plugins\x86_64\
set BASIS_SPIKE_AUTOTEST=1
Unity.exe -batchmode -projectPath <host> -executeMethod SpikeSetup.AutoPlayTest -logFile Logs\autotest.log
```

The dll ships committed inside the package `Runtime/Plugins/`
(rebuild+copy after native changes), so every referencing project loads
the same binary.

Exit 0 = pass (Playing — or Ended at the fixture's full position, since
the startup/seek burst finishes the 6 s fixture inside the test window —
plus >60 presents, >1 s of audio pulled, seek exercised).

`BASIS_SPIKE_DSP_RATE=44100` additionally forces the device DSP rate
below the fixture's 48 kHz, so the run exercises the component's
stream→DSP resample path (the path Android devices hit for real) on
Windows; the same pass bar applies.

### Interactive pass in Basis

The Basis checkout references the package as a local file dependency in
`Packages/manifest.json`. In the Basis editor: open any scene, run
**Basis → Tools → Media Player → Test Scene → One Player (Stereo)** (the shipped
prefab on Basis's initialization scene, a repo fixture prefilled and both
captures armed), enter play mode, and drive it from the inspector (state
readout, play/pause/seek). Swap the URL for the
HTTP(S) rows. Run by hand; report state transitions, A/V sync, seek
behaviour and teardown on scene exit.
### The C player as differential oracle

The C engine this replaced is no longer in this repo. Where a demux question
wants a second opinion, its conformance tool still runs from a checkout of the
branch that carried it (`tools/media-conformance`: `basis_demux_dump` +
`demux_gate.py`), against the same ffprobe oracle semantics `bm-probe
conformance` uses here:

```
# C side (Git Bash, needs cc + ffmpeg):
cd <checkout>/tools/media-conformance && ./build.sh && python demux_gate.py <fixtures-dir>
# Rust side:
cargo run -p bm-probe -- conformance <fixtures-dir>
```

Divergence from ffprobe fails either gate; passing both on one fixture set is
byte-level agreement on every payload ffprobe hashes. That tool's
`gen_fixtures.sh` matrix (faststart / trailing-moov / fragmented, and the codec
spread) is the richer shared corpus.

## Known limitations

Current state, not a plan. Each is something the engine does not do, phrased so
a reader can tell a deliberate refusal from an unbuilt path.

- **The A/V offset's flush race has no automated row.** The offset is computed
  on the audio thread and gated on the presentation origin, which is cleared
  where the generation advances — ahead of both Flush sends, so neither worker
  can observe the new timeline against the old origin, and a pool lease carries
  the generation it was published under so an in-flight render cannot re-arm
  from a retired timeline. Both predicates are pinned by unit rows, and
  `a_seek_clears_the_offset_and_re_arms_on_the_new_timeline` pins the end-to-end
  clear and re-arm. **None of them pins the interleaving.** Reverting the
  clear-to-the-generation-bump leaves every one of them green: the video thread
  reaches its own Flush fast enough that the sentinel appears regardless, and
  holding it back long enough to lose the race needs a pause point in
  `run_video` that does not exist. Adding one is test-only instrumentation on
  the media path, judged not worth it for a value nothing steers on — so the
  ordering is carried by the code and its comments rather than by a row, and a
  change to the seek path's ordering will not be caught here.
- Nothing cancels a hostname resolution once the platform resolver has
  started one. The ceiling on a resolve bounds how long a caller waits,
  and the cap on resolves in flight bounds how many stuck lookups can
  occupy pool threads at once, but a lookup that has begun runs to
  whatever end the OS gives it. Closing that would mean resolving in
  process rather than through the OS, which costs the host's own
  configuration — search domains, hosts file, split DNS on a VPN.
- A resolve queued behind that cap is refused at the same ceiling rather
  than waiting for a permit indefinitely, so sustained hostile resolves
  can turn a legitimate open into a typed resolve failure. It is a
  deliberate trade against the alternative, which is unbounded.
- The 7.1 fixtures stop at the decoder on the engine side:
  `sine-48k-71.flac` decodes 8ch through claxon, `sine-48k-71.aac` pins
  the ADTS channel-screen refusal, and the PcmRing carries the 8-channel
  interleave. Everything above that is the managed side's — the
  per-speaker splitter, the eight-output rig and the ITU-coefficient
  stereo downmix (4ch/7ch layouts fall back to the front pair) — and is
  covered by the package's own test document, not here.
- A split pair whose two sources disagree about where their timelines
  start by more than the decoder cushion is refused rather than played.
  The Bank measures how much it holds as one span across everything
  banked, so the gap between the origins counts as depth it does not
  have: past the cushion it reads as full from the trailing leg's first
  access unit onwards, both legs park on it, release never drains the
  cursor forward because the clock is waiting on a video frame the
  decoder never gets the input to make, and nothing recovers short of
  closing the session. The refusal lands the moment the second leg
  latches its baseline, and applies before the first seek only — a landed
  seek re-latches both baselines wherever each demuxer could stop, which
  is no longer a statement about where either container begins. It is
  also why the lead cap's own behaviour across a seek is pinned by a unit
  test on the predicate rather than by a session: the pair that would
  exercise it end to end is the pair that is refused. Legs that agree on
  their origin play whatever that origin is, which is the shape an
  adaptive ladder serves, both renditions being cuts of one master.
  Reconciling a mismatched pair instead — rebasing the trailing leg onto
  the leading one through the offset the Bank already applies at a
  discontinuity — is not built.
- Raw TS file/stream seek returns Unsupported; HLS-TS VOD seeks by
  segment index, which landed with HLS.
- HLS carries no EXT-X-KEY (encrypted), EXT-X-BYTERANGE, or I-frame-only
  playlists (typed refusals), no ABR (the master playlist's
  highest-bandwidth variant is chosen once), and fetches whole segments
  (bounded 64 MiB) rather than streaming them; live position reporting
  inherits the segment timeline's own offsets.
- Every raw-audio lane seeks, none of them exactly. MP3 goes through its
  Xing/VBRI table (CBR fallback where there is none) and Ogg Opus by
  granule bisection, both reporting the landing at the request. FLAC
  bisects over confirmed frame headers, or reads its SEEKTABLE where the
  file carries one, and reports the sample number the landed header
  states. ADTS has neither an index nor a stated length, so its duration
  and its landing both come off the byte rate averaged over the leading
  frames, and the landing rounds down to a frame.
- Decode breadth gaps: no VP9 software floor (libvpx build infra on
  MSVC is unresolved — VP9 is platform-MFT-only, refused where the
  extension is absent); Opus is mono/stereo only (no multistream
  decoder — surround Opus refuses typed); AV1 output above 8-bit 4:2:0
  refuses (no P010 path). AV1
  hardware decode is future async/D3D11VA work — the Store AV1
  extension blocks inside ProcessInput under sync driving, so rav1d is
  the primary AV1 route.
- RTSP `rtsp://` negotiates UDP (media-rtp reorder/RTCP-RR under
  retina's signalling; retina's own UDP path is unused — no reorder, no
  receiver reports, and real servers kill RR-less sessions) with
  TCP-interleaved fallback; `rtspt://` pins TCP. Sessions carry no
  credentials; the host's resolved addresses are vetted before the
  client dials, and the SETUP response's server-controlled UDP `source`
  address is vetted before any datagram is sent to it. UDP-side
  multicast is not offered. H.264 + AAC only; video dts = pts (RTP
  carries presentation time; these lanes do not reorder B-frames).
- WHEP is H.264 + Opus only (deliberately offered as such — codecs the
  decode factories would refuse are cleaner refused at negotiation);
  `Link rel="ice-server"` entries are parsed, vetted-surfaced and
  reported but not used for STUN/TURN gathering (a receive-only client
  that initiates every connectivity check needs no srflx candidate;
  TURN-only topologies fail as ICE-disconnected). Signalling carries no
  authentication headers yet. The `whep_signal` fuzz target is
  feature-gated (`--features whep`) because str0m's Linux crypto
  backend compiles aws-lc-sys (needs cmake on the fuzz host).
- TS fixtures derive from the MP4 fixture
  (`ffmpeg -i fixtures/h264-aac-640x360-30fps.mp4 -c copy -f mpegts …`);
  the m2ts LPCM fixture is synthesised
  (`… -c:v libx264 -c:a pcm_bluray -f mpegts -mpegts_m2ts_mode 1 …`).
