[package]
name = "media-present"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
media-decode = { path = "../media-decode" }

[target.'cfg(windows)'.dependencies]
windows = { version = "0.62.2", features = ["Win32_Foundation", "Win32_Graphics_Direct3D", "Win32_Graphics_Direct3D_Fxc", "Win32_Graphics_Direct3D11", "Win32_Graphics_Dxgi", "Win32_Graphics_Dxgi_Common", "Win32_Security"] }

# ash rides the same hand-rolled function-loading discipline the M0 spike
# validated (Unity's vkGetInstanceProcAddr, no loader): types + fn
# pointer tables only.
[target.'cfg(target_os = "android")'.dependencies]
ash = { version = "0.38", default-features = false, features = ["debug"] }

[target.'cfg(windows)'.dev-dependencies]
media-demux = { path = "../media-demux" }
media-clock = { path = "../media-clock" }
decode-mf = { path = "../media-decode/decode-mf" }

[lints]
workspace = true
